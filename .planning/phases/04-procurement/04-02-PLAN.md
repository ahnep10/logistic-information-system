---
phase: 04-procurement
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/(protected)/purchase-orders/page.tsx
  - app/(protected)/purchase-orders/purchase-orders-client.tsx
autonomous: true
requirements: [PROC-05]

must_haves:
  truths:
    - "Any authenticated user (Manager or Staff) can view the full purchase order list, filtered by status tab (All/Draft/Ordered/Received) — D-14/D-18"
    - "Each row shows PO # (formatted PO-0001, never the raw cuid), Supplier, Status badge, Total, Created date, Created By — D-19/D-21"
    - "Clicking a row navigates to that PO's detail page"
  artifacts:
    - path: "app/(protected)/purchase-orders/page.tsx"
      provides: "Server Component replacing the Phase 1 stub; fetches all POs and converts Decimal totalAmount before passing to the client"
      contains: "PurchaseOrdersClient"
    - path: "app/(protected)/purchase-orders/purchase-orders-client.tsx"
      provides: "Tabs status filter + table, default export PurchaseOrdersClient"
      exports: ["default"]
  key_links:
    - from: "app/(protected)/purchase-orders/page.tsx"
      to: "app/(protected)/purchase-orders/purchase-orders-client.tsx"
      via: "props: purchaseOrders array with totalAmount already converted via .toNumber() (D-23)"
      pattern: "totalAmount.toNumber\\(\\)"
    - from: "purchase-orders-client.tsx"
      to: "app/(protected)/purchase-orders/[id]/page.tsx (built in 04-04)"
      via: "each table row links to /purchase-orders/{po.id}"
      pattern: "purchase-orders/\\$\\{"
---

<objective>
Replace the Phase 1 one-line stub at `app/(protected)/purchase-orders/page.tsx` with the real PO list page: a server-fetched, Decimal-converted array of purchase orders rendered through a client-side status-filter Tabs component (D-18), matching the exact table columns and copy specified in 04-UI-SPEC.md Screen 1.

Purpose: Delivers the "view" half of PROC-05 (list + filter) as a standalone vertical slice — no dependency on the create/detail flows, so it can execute in parallel with 04-03 in the same wave.
Output: A working `/purchase-orders` list page with All/Draft/Ordered/Received filtering, row-click navigation, and both empty-state variants (database-empty vs. filtered-empty).
</objective>

## Artifacts This Phase Produces

**Prisma schema (04-01):** `enum POStatus`; `model PurchaseOrder`; `model PurchaseOrderLineItem`; `StockTransaction.purchaseOrderId` FK; back-relations on `Supplier`/`User`/`Product`.
**Validation (04-01) — `lib/validations/purchase-order.ts`:** `lineItemSchema`, `createPurchaseOrderSchema`, `confirmPurchaseOrderSchema`, `receivePurchaseOrderSchema`, `assertPOEditable`.
**Utils (04-01):** `lib/utils/po-status.ts` (`getStatusBadge`), `lib/utils/po-number.ts` (`formatPONumber`).
**Tests (04-01):** `tests/purchase-orders.test.ts`.
**Server Actions (04-03/04-04) — `actions/purchase-orders.ts`:** `createDraftPurchaseOrder`, `updateDraftPurchaseOrder`, `confirmPurchaseOrder`, `receivePurchaseOrder`, `deletePurchaseOrder`.
**UI — list (this plan):** `app/(protected)/purchase-orders/page.tsx`, `purchase-orders-client.tsx` (default export `PurchaseOrdersClient`).
**UI — create (04-03):** `app/(protected)/purchase-orders/new/page.tsx`, `po-form-client.tsx` (default export `PurchaseOrderForm`).
**UI — detail (04-04):** `app/(protected)/purchase-orders/[id]/page.tsx`, `po-detail-client.tsx` (default export `PurchaseOrderDetailClient`).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-procurement/04-UI-SPEC.md
@.planning/phases/04-procurement/04-PATTERNS.md

@app/(protected)/inventory/page.tsx
@app/(protected)/suppliers/suppliers-client.tsx

<interfaces>
<!-- From 04-01 (must exist before this plan runs): -->
`lib/utils/po-status.ts` exports `getStatusBadge(status: "DRAFT"|"ORDERED"|"RECEIVED"): { label: string; className: string }`.
`lib/utils/po-number.ts` exports `formatPONumber(poNumber: number): string` → e.g. `"PO-0001"`.

Server Component fetch/serialize pattern to replicate (from `app/(protected)/inventory/page.tsx` and RESEARCH.md Pattern 2 — D-23):
```
const purchaseOrders = await prisma.purchaseOrder.findMany({
  orderBy: { createdAt: "desc" },
  include: { supplier: { select: { name: true } }, createdBy: { select: { name: true } } },
})
const serialized = purchaseOrders.map((po) => ({ ...po, totalAmount: po.totalAmount.toNumber() }))
```

Tabs-filter + table pattern to replicate (from `app/(protected)/suppliers/suppliers-client.tsx`, full file read in this session — `useState<FilterTab>`, `Tabs`/`TabsList`/`TabsTrigger` from `@/components/ui/tabs`, filter derived via `.filter()` on the already-fetched array, `Card`+`Table` wrapping, empty-state block per filter branch).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purchase orders list — Server Component fetch (D-19, D-23)</name>
  <files>app/(protected)/purchase-orders/page.tsx</files>
  <action>
Replace the current one-line stub entirely. Fetch `prisma.purchaseOrder.findMany({ orderBy: { createdAt: "desc" }, include: { supplier: { select: { name: true } }, createdBy: { select: { name: true } } } })`, selecting `id`, `poNumber`, `status`, `totalAmount`, `createdAt` implicitly via the default field set. Map the result through `.toNumber()` on `totalAmount` before passing to the client component (D-23 — this Server Component is the RSC boundary; a raw `Decimal` instance passed as a prop throws "Only plain objects can be passed to Client Components from Server Components" per RESEARCH.md Pitfall 1). Render an `<h1>` "Purchase Orders" is NOT needed here since the H1 lives inside `PurchaseOrdersClient` per the suppliers-page precedent (page.tsx is a thin fetch-and-pass shell, matching `app/(protected)/suppliers/page.tsx`'s existing shape — confirm this by checking that file's shape if unsure, otherwise default to rendering `<PurchaseOrdersClient purchaseOrders={serialized} />` directly). Import and render `PurchaseOrdersClient` from `./purchase-orders-client`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Stub replaced; page fetches all POs, converts every Decimal field, and renders `PurchaseOrdersClient`; no TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: PurchaseOrdersClient — status Tabs filter + table (D-18, D-19)</name>
  <files>app/(protected)/purchase-orders/purchase-orders-client.tsx</files>
  <action>
Create the default-exported `PurchaseOrdersClient({ purchaseOrders })` client component, modeled directly on `suppliers-client.tsx`'s Tabs-filter shape (read in this session): `type FilterTab = "all" | "draft" | "ordered" | "received"`, `useState<FilterTab>("all")`, filter the already-fetched array by comparing `po.status.toLowerCase()` against the active tab (or `"all"`). Page header: "Purchase Orders" (`text-2xl font-semibold`) plus a "Create Purchase Order" button (`bg-primary`, `PackagePlus` icon from `lucide-react`, `Link` to `/purchase-orders/new`) — no role gate, any authenticated user sees it (D-14). `Tabs`/`TabsList`/`TabsTrigger` for All/Draft/Ordered/Received. Table columns exactly per 04-UI-SPEC.md Screen 1: PO # (`formatPONumber(po.poNumber)`), Supplier (`po.supplier.name`), Status (`<Badge className={getStatusBadge(po.status).className}>{getStatusBadge(po.status).label}</Badge>`), Total (`Intl.NumberFormat("id-ID", { style: "currency", currency: "IDR", minimumFractionDigits: 0 }).format(po.totalAmount)`, right-aligned), Created (`new Date(po.createdAt).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" })`), Created By (`po.createdBy.name`). Each `TableRow` is wrapped so the whole row is clickable (`cursor-pointer hover:bg-muted/50`) and navigates via `Link href={`/purchase-orders/${po.id}`}` (use a `Link`-wrapping approach, not a bare `onClick` on a `div`, per 04-UI-SPEC.md Accessibility requirement for keyboard-reachable row navigation). Two empty-state variants per 04-UI-SPEC.md Copywriting Contract: database-empty ("No purchase orders yet" / "Create a purchase order to start tracking procurement.") when `purchaseOrders.length === 0`, and filtered-empty ("No {status} purchase orders" / "Try a different filter tab.") when the active tab yields zero rows but POs exist overall. Use `ClipboardList` from `lucide-react` for both empty states.
  </action>
  <verify>
    <automated>npm test &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Tabs filter switches the visible rows client-side with no server round-trip; table renders all six columns with correct formatting; both empty-state copy variants match 04-UI-SPEC.md verbatim; clicking anywhere on a row navigates to `/purchase-orders/{id}`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Component | Read-only page; no user input crosses into a query parameter or mutation here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Information Disclosure | `/purchase-orders` route access | low | accept | Route sits under `app/(protected)/`, already gated by the Phase 1 middleware session check for every user (Manager and Staff both see full list per D-14 — no PO data is more sensitive to Staff than Manager in this domain, so no additional row-level filtering is warranted) |
| T-04-05 | Tampering | Passing raw Prisma `Decimal` to a Client Component | medium | mitigate | Explicit `.toNumber()` conversion in the Server Component before the props cross the RSC boundary (D-23) — otherwise this throws at runtime, not compile time |

</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm test` (full suite) exits 0 — confirms no regression to existing Phase 1-3 tests.
- Manual: visit `/purchase-orders`, confirm empty-state copy shows correctly with zero POs (true until 04-03 lands).
</verification>

<success_criteria>
`/purchase-orders` renders the full list with working status-tab filtering, correct column formatting (PO number, currency, dates), and row-click navigation to the detail route — satisfying the list half of PROC-05.
</success_criteria>

<output>
Create `.planning/phases/04-procurement/04-02-SUMMARY.md` when done
</output>
