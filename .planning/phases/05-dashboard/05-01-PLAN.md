---
phase: 05-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - lib/utils/dashboard.ts
  - tests/dashboard.test.ts
  - app/(protected)/dashboard/page.tsx
  - app/(protected)/dashboard/dashboard-client.tsx
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03]

must_haves:
  truths:
    - "A manager visiting /dashboard sees 4 KPI tiles with live counts: active products, active suppliers, movements recorded today (UTC calendar day), and low-stock items"
    - "The PO status panel renders a 3-slice Recharts pie chart (Draft/Ordered/Received) when at least one PO exists anywhere in the system, or a 'No purchase orders yet' empty state when zero POs exist"
    - "Clicking the Low Stock KPI tile navigates to /products?stock=low"
    - "Clicking a pie slice navigates to /purchase-orders?status={STATUS} using the clicked slice's status"
  artifacts:
    - "app/(protected)/dashboard/page.tsx"
    - "app/(protected)/dashboard/dashboard-client.tsx"
    - "lib/utils/dashboard.ts"
    - "tests/dashboard.test.ts"
  key_links:
    - "dashboard/page.tsx's 5 parallel Prisma queries -> DashboardClient props (plain numbers only, no Decimal, no Prisma model instances)"
    - "Low-stock KPI <Link href=\"/products?stock=low\"> -> Plan 05-02 must apply the matching filter on /products for this drill-down to show filtered results"
    - "Pie slice onClick -> router.push(`/purchase-orders?status=${status}`) -> Plan 05-03 must read this param for the Tab to pre-select"
---

<objective>
Replace the Phase 1 stub at `app/(protected)/dashboard/page.tsx` with the real manager dashboard: 4 live KPI tiles (D-06/D-07) and a Recharts PO-status pie chart (D-08/D-09), fed by 5 parallel read-only Prisma aggregation queries. This is this phase's first actual `npm install` and first use of Recharts in the codebase.

Purpose: Give managers a single at-a-glance operational health view (DASH-01), with the pie chart doubling as a drill-down entry point into the PO list by status (DASH-03) and the low-stock tile as a drill-down entry point into the filtered product list (DASH-02).
Output: `app/(protected)/dashboard/page.tsx`, `app/(protected)/dashboard/dashboard-client.tsx`, `lib/utils/dashboard.ts`, `tests/dashboard.test.ts`, `recharts@3.9.2` added to `package.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard/05-CONTEXT.md
@.planning/phases/05-dashboard/05-RESEARCH.md
@.planning/phases/05-dashboard/05-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Verify recharts package legitimacy before install</name>
  <files></files>
  <action>Human-verify checkpoint — confirm the `recharts` npm package's legitimacy on npmjs.com before Task 1 runs `npm install recharts@3.9.2`. No file changes in this task.</action>
  <what-built>Nothing yet — this is a pre-install legitimacy gate for a new npm dependency, per RESEARCH.md's Package Legitimacy Audit (`recharts` was automatically flagged `SUS` with reason "too-new").</what-built>
  <how-to-verify>
    RESEARCH.md's investigation found the `SUS` flag is a false positive (triggered by the latest *patch* 3.9.2 being 2 days old, not the package itself — `recharts` has a 10-year registry history since 2015-08-07, 51.3M weekly downloads, and the source repo is the official `github.com/recharts/recharts` org).
    1. Open https://www.npmjs.com/package/recharts in a browser.
    2. Confirm: publisher/maintainers listed match the official Recharts org, weekly downloads are in the tens of millions, and the repository link points to `github.com/recharts/recharts`.
    3. Confirm no unexpected `postinstall` script is declared for version `3.9.2` (RESEARCH.md already confirmed `signals.postinstall: null`).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with `npm install recharts@3.9.2`, or describe concerns to halt.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Install recharts + pure dashboard helper functions</name>
  <files>package.json, package-lock.json, lib/utils/dashboard.ts, tests/dashboard.test.ts</files>
  <read_first>
    lib/utils/severity.ts (this codebase's convention for a small, single-purpose `lib/utils/*.ts` file: named exports, top-of-file comment citing the source decision)
    lib/utils/po-status.ts (second example of the same convention, including a typed enum-like union)
    tests/catalog.test.ts (existing Vitest convention for pure-logic unit tests with no mocking — `describe`/`it` blocks, no `vi.mock` needed for pure functions)
  </read_first>
  <behavior>
    - `getTodayUtcRange()`: with system time frozen at `2026-07-06T15:30:00.000Z` (`vi.setSystemTime`), returns `{ start, end }` where `start.toISOString() === "2026-07-06T00:00:00.000Z"` and `end.toISOString() === "2026-07-06T23:59:59.999Z"`.
    - `getTodayUtcRange()`: with system time frozen at `2026-01-01T00:00:00.500Z` (a UTC day boundary edge case), still returns `start` at `2026-01-01T00:00:00.000Z` and `end` at `2026-01-01T23:59:59.999Z` for that same UTC date (not the previous/next day).
    - `fillPoStatusCounts([])` returns `{ DRAFT: 0, ORDERED: 0, RECEIVED: 0 }`.
    - `fillPoStatusCounts([{ status: "DRAFT", _count: { status: 3 } }, { status: "RECEIVED", _count: { status: 1 } }])` returns `{ DRAFT: 3, ORDERED: 0, RECEIVED: 1 }` — the missing `ORDERED` group defaults to 0, matching `prisma.purchaseOrder.groupBy`'s documented behavior of omitting zero-count groups.
  </behavior>
  <action>
    Run `npm install recharts@3.9.2` (only after the preceding checkpoint is approved) — this updates `package.json`'s `dependencies` and `package-lock.json`. Do not add `--legacy-peer-deps`; RESEARCH.md confirms `recharts@3.9.2`'s peer dependencies (`react`/`react-dom`/`react-is` `^16.8.0 || ^17.0.0 || ^18.0.0 || ^19.0.0`) already cover this project's React 19.1.0.

    Create `lib/utils/dashboard.ts` with a top-of-file comment citing "Dashboard KPI helpers — source: 05-CONTEXT.md D-10/D-11, 05-RESEARCH.md Code Examples". Export two functions:
    - `getTodayUtcRange(): { start: Date; end: Date }` — computes UTC calendar-day boundaries for "now" using `Date.UTC(now.getUTCFullYear(), now.getUTCMonth(), now.getUTCDate(), 0, 0, 0, 0)` for `start` and the same with `23, 59, 59, 999` for `end`, per D-10.
    - `fillPoStatusCounts(groups: { status: "DRAFT" | "ORDERED" | "RECEIVED"; _count: { status: number } }[]): { DRAFT: number; ORDERED: number; RECEIVED: number }` — initializes a `{ DRAFT: 0, ORDERED: 0, RECEIVED: 0 }` record, then loops over `groups` assigning `counts[g.status] = g._count.status`, per D-11 and RESEARCH.md's groupBy zero-fill pattern. Export the return type as `export type POStatusCounts = { DRAFT: number; ORDERED: number; RECEIVED: number }`.

    Create `tests/dashboard.test.ts` covering exactly the 4 cases in `<behavior>` above. Use `vi.useFakeTimers()` / `vi.setSystemTime(new Date("..."))` (Vitest's built-in fake timer API, already available — no new dependency) and `vi.useRealTimers()` in an `afterEach` to avoid leaking frozen time into later tests in the same file (Task 2 of this plan appends more tests to this same file).
  </action>
  <acceptance_criteria>
    - `npm ls recharts` reports `recharts@3.9.2` installed.
    - `package.json`'s `dependencies` object contains `"recharts": "^3.9.2"` (or the exact pinned version npm writes).
    - `lib/utils/dashboard.ts` exports `getTodayUtcRange` and `fillPoStatusCounts` with the signatures specified above.
    - `npx vitest run tests/dashboard.test.ts -t "UTC day boundary"` passes (or equivalent test-name substring covering the `getTodayUtcRange` cases).
    - `npx vitest run tests/dashboard.test.ts -t "groupBy zero-fill"` passes (or equivalent test-name substring covering the `fillPoStatusCounts` cases).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/dashboard.test.ts</automated>
  </verify>
  <done>`recharts@3.9.2` is an installed dependency; `lib/utils/dashboard.ts` exists with both helpers; all 4 behavior cases pass in `tests/dashboard.test.ts`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dashboard page (5 Prisma queries) + Recharts client component</name>
  <files>app/(protected)/dashboard/page.tsx, app/(protected)/dashboard/dashboard-client.tsx, tests/dashboard.test.ts</files>
  <read_first>
    app/(protected)/dashboard/page.tsx (current 3-line stub being replaced — confirm it is the synchronous `export default function DashboardPage()` returning `<h1>Dashboard</h1>`)
    app/(protected)/purchase-orders/page.tsx (parallel-fetch + serialize-then-pass-to-client shape to mirror, minus any Decimal handling — this phase's queries are counts/groupBy only)
    app/(protected)/purchase-orders/purchase-orders-client.tsx (Card usage, `Link`-wrapped navigation convention, import ordering convention)
    lib/utils/po-status.ts (the `DRAFT`/`ORDERED`/`RECEIVED` status family and existing badge color mapping this pie chart's `STATUS_COLORS` must visually match)
    node_modules/.prisma/client/index.d.ts (grep `ProductFieldRefs` — confirms `prisma.product.fields.reorderThreshold` is a real, typed `FieldRef<"Product", 'Int'>` on the installed 6.19.3 client; no raw SQL needed)
    tests/purchase-orders.test.ts lines 1-52 (existing `vi.mock("@/lib/prisma", ...)` convention for mocking specific Prisma methods without a real DB connection)
  </read_first>
  <behavior>
    - `DashboardPage()` (no args — this page has no `searchParams`) calls, in a single `Promise.all`: `prisma.product.count({ where: { isActive: true } })`, `prisma.supplier.count({ where: { isActive: true } })`, `prisma.stockTransaction.count({ where: { createdAt: { gte, lte } } })` using the `{ start, end }` from `getTodayUtcRange()`, `prisma.product.count({ where: { isActive: true, currentStock: { lte: prisma.product.fields.reorderThreshold } } })`, and `prisma.purchaseOrder.groupBy({ by: ["status"], _count: { status: true } })`.
    - The returned JSX element's `props` (i.e. `(await DashboardPage()).props`) contains `totalProducts`, `totalSuppliers`, `movementsToday`, `lowStockCount` matching the 4 mocked count values exactly, and `poStatusCounts` equal to `fillPoStatusCounts(...)` applied to the mocked `groupBy` result (e.g. mocked groups `[{status:"DRAFT",_count:{status:2}}]` produce `poStatusCounts: {DRAFT:2, ORDERED:0, RECEIVED:0}`).
    - The `stockTransaction.count` call's `where.createdAt.gte`/`.lte` are `Date` instances whose `.toISOString()` match `getTodayUtcRange()`'s current-UTC-day boundaries (assert via `vi.setSystemTime` in the test, same fake-timer approach as Task 1).
  </behavior>
  <action>
    Rewrite `app/(protected)/dashboard/page.tsx` as an `async function DashboardPage()` (default export, no props — manager-only access is already enforced by `middleware.ts`'s `MANAGER_ROUTES`, confirmed present, no new auth code needed here). Import `{ prisma }` from `@/lib/prisma` and `{ getTodayUtcRange, fillPoStatusCounts }` from `@/lib/utils/dashboard`. Run the 5 queries listed in `<behavior>` inside one `Promise.all([...])`, destructuring results as `[totalProducts, totalSuppliers, movementsToday, lowStockCount, poStatusGroups]`. Compute `const poStatusCounts = fillPoStatusCounts(poStatusGroups)`. Return `<DashboardClient totalProducts={totalProducts} totalSuppliers={totalSuppliers} movementsToday={movementsToday} lowStockCount={lowStockCount} poStatusCounts={poStatusCounts} />`.

    Create `app/(protected)/dashboard/dashboard-client.tsx` as a `"use client"` component. Import `useRouter` from `next/navigation`, `Link` from `next/link`, `{ Package, Truck, Activity, AlertTriangle, ClipboardList }` from `lucide-react`, `{ PieChart, Pie, Cell, Tooltip, Legend, ResponsiveContainer }` from `recharts`, and `{ Card }` from `@/components/ui/card`. Define `const STATUS_COLORS: Record<"DRAFT"|"ORDERED"|"RECEIVED", string> = { DRAFT: "#64748b", ORDERED: "#3b82f6", RECEIVED: "#22c55e" }` (matches `lib/utils/po-status.ts`'s slate/blue/green badge families, per 05-UI-SPEC.md Color section). Props interface `DashboardClientProps` = `{ totalProducts: number; totalSuppliers: number; movementsToday: number; lowStockCount: number; poStatusCounts: { DRAFT: number; ORDERED: number; RECEIVED: number } }`.

    Render, top to bottom (05-UI-SPEC.md Screen 1 layout): an `<h1 className="text-2xl font-semibold mb-6">Dashboard</h1>`; a `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-6">` containing 4 `<Card className="p-4 flex items-center gap-3">` tiles — Package/`text-muted-foreground` for "Active Products", Truck/`text-muted-foreground` for "Active Suppliers", Activity/`text-muted-foreground` for "Movements Today" (all three plain, non-interactive), and the 4th tile wrapped in `<Link href="/products?stock=low">` with `<Card className="p-4 flex items-center gap-3 hover:bg-muted/50 cursor-pointer transition-colors">`, `AlertTriangle` icon in `text-amber-600`, label "Low Stock Items". Each tile's number uses `text-2xl font-semibold`, label uses `text-sm text-muted-foreground`, icon size `h-8 w-8`.

    Below the grid, a `<Card className="p-4">` with `<h2 className="text-xl font-semibold mb-4">Purchase Order Status</h2>`. Compute `const hasAnyPO = poStatusCounts.DRAFT + poStatusCounts.ORDERED + poStatusCounts.RECEIVED > 0`. If `hasAnyPO` is false, render the empty state: `ClipboardList` icon (`w-8 h-8 text-muted-foreground/30 mb-3`), "No purchase orders yet" (`text-sm font-medium`), "Create a purchase order to see status breakdown here." (`text-sm text-muted-foreground mt-1`) — exact copy match with `purchase-orders-client.tsx`'s own empty state. If `hasAnyPO` is true, render `<ResponsiveContainer width="100%" height={300}><PieChart><Pie data={pieData} dataKey="value" nameKey="name" onClick={handleSliceClick} cursor="pointer">{pieData.map(...) => <Cell key={entry.status} fill={STATUS_COLORS[entry.status]} />}</Pie><Tooltip /><Legend /></PieChart></ResponsiveContainer>` where `pieData = [{name:"Draft",status:"DRAFT",value:poStatusCounts.DRAFT},{name:"Ordered",status:"ORDERED",value:poStatusCounts.ORDERED},{name:"Received",status:"RECEIVED",value:poStatusCounts.RECEIVED}]`.

    Define `handleSliceClick(data: any)` reading `const status = data?.payload?.status ?? data?.status` (defensive against Recharts 3.x's exact `onClick` payload shape, per RESEARCH.md Open Question A1 — Recharts nests the original data entry under `.payload` in some but not all callback sites) then, if `status` is truthy, `router.push(\`/purchase-orders?status=${status}\`)`.

    Append to `tests/dashboard.test.ts`: mock `@/lib/prisma` with `product: { count: vi.fn(), fields: { reorderThreshold: "REORDER_THRESHOLD_FIELDREF" } }`, `supplier: { count: vi.fn() }`, `stockTransaction: { count: vi.fn() }`, `purchaseOrder: { groupBy: vi.fn() }`. Import `DashboardPage` via `const DashboardPage = (await import("@/app/(protected)/dashboard/page")).default` (top-level dynamic import, matching `tests/purchase-orders.test.ts`'s existing convention for importing modules after `vi.mock` calls). Assert the 5 call shapes and the returned element's `props` per `<behavior>` above.
  </action>
  <acceptance_criteria>
    - `app/(protected)/dashboard/page.tsx` has zero occurrences of the literal string `<h1>Dashboard</h1>` (old stub fully replaced).
    - `grep -c "prisma.product.fields.reorderThreshold" app/(protected)/dashboard/page.tsx` returns at least 1.
    - `app/(protected)/dashboard/dashboard-client.tsx` starts with `"use client"` and imports from `"recharts"`.
    - `npx vitest run tests/dashboard.test.ts` passes with all behavior cases from both Task 1 and Task 2 green.
    - `npx tsc --noEmit` produces no new type errors attributable to these two files.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/dashboard.test.ts</automated>
    <human-check>Log in as a Manager, visit /dashboard. Confirm all 4 KPI tiles show plausible non-error numbers, the PO status panel shows a pie chart (or the "No purchase orders yet" empty state if no POs exist), hovering a slice shows a tooltip, and clicking a slice or the Low Stock tile navigates without a console error. Full click-through drill-down correctness (whether the target pages actually filter) is verified in 05-02-PLAN.md and 05-03-PLAN.md's own checks plus the end-of-phase UAT.</human-check>
  </verify>
  <done>Dashboard renders 4 live KPI tiles and a PO status pie chart (or empty state) from real Prisma data; low-stock tile links to /products?stock=low; pie slices call router.push to /purchase-orders?status={STATUS}; `npm test` includes and passes all `tests/dashboard.test.ts` cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → project dependencies | `npm install recharts@3.9.2` pulls third-party code into `node_modules`; that code executes inside this app's build and runtime process |
| Manager browser → `/dashboard` Server Component | Requires an authenticated MANAGER session, enforced by `middleware.ts`'s `MANAGER_ROUTES` before this page's code ever runs — no new auth logic added this phase |
| `dashboard/page.tsx` → PostgreSQL via Prisma | 5 read-only aggregation queries; this page declares no `searchParams`, so no user-controlled string reaches any of these queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | `npm install recharts@3.9.2` (package.json, node_modules) | high | mitigate | RESEARCH.md's Package Legitimacy Audit already confirms recharts is a 10-year-old, 51.3M-weekly-download, official-repo package (the automated `SUS` flag is a confirmed false positive from a "too-new patch version" heuristic); this plan's Task 0 blocking `checkpoint:human-verify` requires a human to confirm npmjs.com/package/recharts before Task 1 runs the install |
| T-05-01 | Information Disclosure | dashboard/page.tsx KPI aggregation queries | low | accept | `/dashboard` is MANAGER-only (verified via direct read of `middleware.ts`); the 4 KPI counts and PO status breakdown reveal no PII — only aggregate operational totals that are also independently derivable by any authenticated user browsing `/products`, `/inventory`, and `/purchase-orders` directly |
| T-05-02 | Denial of Service | dashboard-client.tsx Recharts pie chart on all-zero PO data | low | mitigate | `hasAnyPO` check renders the `ClipboardList` empty state instead of feeding Recharts a degenerate all-zero-value pie (which can render incorrectly or throw), per 05-UI-SPEC.md Screen 1 |

</threat_model>

<verification>
- `npm ls recharts` shows `3.9.2` installed.
- `npx vitest run tests/dashboard.test.ts` — all cases green (UTC boundary, groupBy zero-fill, KPI query shapes, low-stock FieldRef shape).
- `npx tsc --noEmit` — no new errors in `app/(protected)/dashboard/` or `lib/utils/dashboard.ts`.
- Manual: `/dashboard` renders without console errors for a MANAGER session; low-stock tile and pie slices navigate to the expected URLs.
</verification>

<success_criteria>
- DASH-01: `/dashboard` displays 4 live KPI tiles sourced from real Prisma counts.
- DASH-02 (partial — link only, filter lands in 05-02): Low Stock tile links to `/products?stock=low`.
- DASH-03 (partial — chart + click only, filter target lands in 05-03): PO status pie chart renders Draft/Ordered/Received counts and slices navigate to `/purchase-orders?status={STATUS}`.
</success_criteria>

<output>
Create `.planning/phases/05-dashboard/05-01-SUMMARY.md` when done
</output>
