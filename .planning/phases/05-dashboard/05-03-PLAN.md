---
phase: 05-dashboard
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(protected)/purchase-orders/page.tsx
  - app/(protected)/purchase-orders/purchase-orders-client.tsx
  - tests/purchase-orders.test.ts
autonomous: true
requirements: [DASH-03]

must_haves:
  truths:
    - "/purchase-orders?status=DRAFT|ORDERED|RECEIVED pre-selects the matching Tab on load (D-09)"
    - "Any ?status value other than the exact literals DRAFT/ORDERED/RECEIVED (or its absence) defaults to the All tab — never an error"
  artifacts:
    - "app/(protected)/purchase-orders/page.tsx"
    - "app/(protected)/purchase-orders/purchase-orders-client.tsx"
    - "tests/purchase-orders.test.ts (extended)"
  key_links:
    - "searchParams.status (validated against POStatus union) -> initialFilter prop (lowercased) -> useState<FilterTab>(initialFilter ?? \"all\") seed in purchase-orders-client.tsx"
---

<objective>
Add `?status={STATUS}` URL-param entry point to `/purchase-orders`, which today has NO `searchParams` handling at all (RESEARCH.md Pitfall 1 — CONTEXT.md's framing that this "already exists" is corrected here: only the client-side Tabs UI/filtering exists, with zero URL-param wiring). `page.tsx` validates the param against the `POStatus` enum and seeds `purchase-orders-client.tsx`'s existing `useState<FilterTab>` from it.

Purpose: This is the target-page half of the dashboard's PO-status drill-down (DASH-03) — the dashboard's pie chart (05-01-PLAN.md) navigates here via `router.push`, but this plan is what actually makes the Tab pre-selection real. This page works standalone via a manually-typed URL even before 05-01 lands.
Output: `app/(protected)/purchase-orders/page.tsx` (searchParams + validated `initialFilter` prop), `app/(protected)/purchase-orders/purchase-orders-client.tsx` (seeded `useState`), `tests/purchase-orders.test.ts` (extended).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard/05-CONTEXT.md
@.planning/phases/05-dashboard/05-RESEARCH.md
@.planning/phases/05-dashboard/05-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validated ?status= searchParams on /purchase-orders</name>
  <files>app/(protected)/purchase-orders/page.tsx, tests/purchase-orders.test.ts</files>
  <read_first>
    app/(protected)/purchase-orders/page.tsx (current file, full — 19 lines, no `searchParams` prop exists today; confirms RESEARCH.md Pitfall 1)
    app/(protected)/purchase-orders/purchase-orders-client.tsx lines 33-46 (the `FilterTab` type union and the `useState<FilterTab>("all")` this task's `initialFilter` prop must seed)
    lib/utils/po-status.ts (the canonical `POStatus = "DRAFT" | "ORDERED" | "RECEIVED"` union to validate `searchParams.status` against — case-sensitive, matching the dashboard's `router.push` value)
    tests/purchase-orders.test.ts lines 1-52 (existing `vi.mock("@/lib/prisma", ...)` factory this task extends in place — do NOT add a second `vi.mock("@/lib/prisma", ...)` call; Vitest only honors the first mock factory per module per file)
  </read_first>
  <behavior>
    - `PurchaseOrdersPage({ searchParams: Promise.resolve({ status: "DRAFT" }) })` returns a JSX element whose `props.initialFilter === "draft"`.
    - `PurchaseOrdersPage({ searchParams: Promise.resolve({ status: "ORDERED" }) })` → `props.initialFilter === "ordered"`; `{ status: "RECEIVED" }` → `props.initialFilter === "received"`.
    - `PurchaseOrdersPage({ searchParams: Promise.resolve({ status: "draft" }) })` (lowercase — wrong case per the case-sensitive enum match) → `props.initialFilter === undefined`.
    - `PurchaseOrdersPage({ searchParams: Promise.resolve({ status: "GARBAGE" }) })` and `PurchaseOrdersPage({ searchParams: Promise.resolve({}) })` both → `props.initialFilter === undefined` — never a thrown error (RESEARCH.md Pitfall 2).
    - `prisma.purchaseOrder.findMany`'s call arguments are unchanged by any of the above (this page's fetch query itself is never filtered server-side — only `initialFilter` changes; filtering stays client-side via the existing Tabs `useState`).
  </behavior>
  <action>
    In `app/(protected)/purchase-orders/page.tsx`, add `const VALID_STATUSES = ["DRAFT", "ORDERED", "RECEIVED"] as const`, `type SearchParams = { status?: string }`, `type Props = { searchParams: Promise<SearchParams> }`. Change the default export to `export default async function PurchaseOrdersPage({ searchParams }: Props)`. Await `searchParams` into `params`. Compute `const isValidStatus = (VALID_STATUSES as readonly string[]).includes(params.status ?? "")` (exact case-sensitive match against the 3 literals only). Compute `const initialFilter = isValidStatus ? (params.status!.toLowerCase() as "draft" | "ordered" | "received") : undefined`. Leave the existing `prisma.purchaseOrder.findMany({...})` call and its `serialized` mapping completely unchanged — do not add a `where` clause; filtering remains 100% client-side per 05-UI-SPEC.md Screen 3. Pass the new prop: `<PurchaseOrdersClient purchaseOrders={serialized} initialFilter={initialFilter} />`.

    In `tests/purchase-orders.test.ts`, edit the existing `vi.mock("@/lib/prisma", () => ({ prisma: { ... } }))` factory (around line 29-38) to add `findMany: vi.fn()` inside the existing `purchaseOrder: { ... }` object (alongside the existing `findUnique`, `update`, `deleteMany` keys) — do not create a second `vi.mock` call for the same module. Add `const PurchaseOrdersPage = (await import("@/app/(protected)/purchase-orders/page")).default` alongside the file's existing top-level dynamic imports. Add a new `describe("Purchase Orders Page — status searchParams filter (DASH-03)", () => { ... })` block covering all 5 cases in `<behavior>` above; in each test's `beforeEach`/setup, `vi.mocked(prisma.purchaseOrder.findMany).mockResolvedValue([])` so the `.map((po) => ({...po, totalAmount: po.totalAmount.toNumber()}))` call has nothing to iterate (avoids needing a full Decimal-shaped mock object for these `initialFilter`-focused tests).
  </action>
  <acceptance_criteria>
    - `grep -c "VALID_STATUSES" app/(protected)/purchase-orders/page.tsx` returns at least 1.
    - `grep -c "searchParams" app/(protected)/purchase-orders/page.tsx` returns at least 1 (confirms the previously-absent prop now exists).
    - `npx vitest run tests/purchase-orders.test.ts` passes — both the new DASH-03 describe block AND all pre-existing PROC-01..05 tests in this file remain green (no regression from editing the shared mock factory).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/purchase-orders.test.ts</automated>
  </verify>
  <done>`/purchase-orders?status=X` validates against the `POStatus` enum case-sensitively, computing a lowercased `initialFilter` prop or `undefined` on any invalid/absent value; the underlying Prisma fetch is untouched; the full pre-existing test suite in this file still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Seed Tabs filter from initialFilter prop</name>
  <files>app/(protected)/purchase-orders/purchase-orders-client.tsx</files>
  <read_first>
    app/(protected)/purchase-orders/purchase-orders-client.tsx lines 33-46 (current `PurchaseOrdersClientProps` interface and the `useState<FilterTab>("all")` line this task changes)
    .planning/phases/05-dashboard/05-UI-SPEC.md (Screen 3 section — confirms no new visual elements are needed here; the existing Tabs UI already communicates the active filter, this is purely a one-line state-seed change)
  </read_first>
  <action>
    In `app/(protected)/purchase-orders/purchase-orders-client.tsx`, add `initialFilter?: FilterTab` to the `PurchaseOrdersClientProps` interface. Change the function signature from `export default function PurchaseOrdersClient({ purchaseOrders }: PurchaseOrdersClientProps)` to `export default function PurchaseOrdersClient({ purchaseOrders, initialFilter }: PurchaseOrdersClientProps)`. Change `const [filter, setFilter] = useState<FilterTab>("all")` to `const [filter, setFilter] = useState<FilterTab>(initialFilter ?? "all")`. Make no other changes — the `Tabs`/`TabsList`/`TabsTrigger` JSX (lines 63-74) and all filtering logic (`visiblePurchaseOrders`) are unchanged; the pre-selected Tab is a direct consequence of `filter`'s initial value.
  </action>
  <acceptance_criteria>
    - `grep -c "initialFilter" app/(protected)/purchase-orders/purchase-orders-client.tsx` returns at least 2 (prop declaration + `useState` seed usage).
    - `npx tsc --noEmit` reports no new type errors in this file.
    - `npx vitest run tests/purchase-orders.test.ts` still passes in full (this task's change must not affect any Server Action test in the same file).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/purchase-orders.test.ts</automated>
    <human-check>Visit /purchase-orders?status=ORDERED and confirm the "Ordered" Tab is pre-selected and only Ordered POs show. Visit /purchase-orders?status=bogus and confirm it behaves identically to /purchase-orders (All tab selected, full list, no error).</human-check>
  </verify>
  <done>`purchase-orders-client.tsx` seeds its Tab filter from `initialFilter` when present and valid, defaulting to "all" otherwise; existing Tabs interaction behavior is fully preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser URL bar → `/purchase-orders?status=` searchParams | Any string value is attacker/user-controllable and reaches the Server Component as raw text |
| `page.tsx` → `initialFilter` prop | Only a validated, enum-narrowed value (or `undefined`) crosses into the Client Component — the raw string itself is discarded after validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Denial of Service | `app/(protected)/purchase-orders/page.tsx` `searchParams.status` | high | mitigate | Whitelist-validate against exactly `"DRAFT"` \| `"ORDERED"` \| `"RECEIVED"` (case-sensitive); any other value (lowercase, garbage, absent) resolves `initialFilter` to `undefined`, which `purchase-orders-client.tsx` seeds as `"all"` — never thrown, and never reaches Prisma at all since the fetch query itself is not filtered server-side. Mirrors the fix pattern this phase also applies in 05-02-PLAN.md for `?stock=`, closing the same class of gap tracked as open technical debt on `/inventory` (STATE.md T-03-11). |
| T-05-06 | Information Disclosure | `/purchase-orders?status=X` | low | accept | No new fields or access exposed — `/purchase-orders` is already visible to both MANAGER and STAFF; this only pre-selects an already-existing client-side Tab. |

</threat_model>

<verification>
- `npx vitest run tests/purchase-orders.test.ts` — full file green, including both the new DASH-03 tests and all pre-existing PROC-01..05 tests (no regression from the shared mock-factory edit).
- `npx tsc --noEmit` — no new type errors.
- Manual: `/purchase-orders?status=DRAFT|ORDERED|RECEIVED` pre-selects the matching Tab; any other value behaves identically to no param.
</verification>

<success_criteria>
- DASH-03: `/purchase-orders?status={STATUS}` pre-selects the matching Tab for valid enum values and degrades silently to "All" otherwise.
</success_criteria>

<output>
Create `.planning/phases/05-dashboard/05-03-SUMMARY.md` when done
</output>
