---
phase: 02-catalog
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/textarea.tsx
  - components/ui/tabs.tsx
  - lib/utils/severity.ts
  - lib/validations/category.ts
  - lib/validations/product.ts
  - lib/validations/supplier.ts
  - tests/catalog.test.ts
autonomous: true
requirements:
  - PROD-01
  - PROD-04
  - SUPL-01

must_haves:
  truths:
    - "getSeverityBadge(0, 10) returns label 'Critical' and className containing 'red'"
    - "getSeverityBadge(5, 10) returns label 'Warning' and className containing 'amber'"
    - "getSeverityBadge(11, 10) returns label 'OK' and className containing 'green'"
    - "createProductSchema rejects empty SKU and rejects negative reorderThreshold"
    - "createProductSchema accepts valid {name, sku, categoryId, reorderThreshold: 0}"
    - "createSupplierSchema rejects invalid email and accepts valid supplier input"
    - "tests/catalog.test.ts passes with npm test (unit tests pass; it.todo stubs are pending)"
    - "components/ui/textarea.tsx and components/ui/tabs.tsx exist and are shadcn-managed"
  artifacts:
    - lib/utils/severity.ts
    - lib/validations/category.ts
    - lib/validations/product.ts
    - lib/validations/supplier.ts
    - tests/catalog.test.ts
    - components/ui/textarea.tsx
    - components/ui/tabs.tsx
  key_links:
    - "getSeverityBadge imported by products-client.tsx (Plan 02-04) — must not be renamed"
    - "createCategorySchema / updateCategorySchema imported by actions/categories.ts (Plan 02-03)"
    - "createProductSchema / updateProductSchema imported by actions/products.ts (Plan 02-04)"
    - "createSupplierSchema / updateSupplierSchema imported by actions/suppliers.ts (Plan 02-05)"
    - "textarea.tsx used in suppliers-client.tsx (Plan 02-05); tabs.tsx used in suppliers-client.tsx (Plan 02-05)"
---

<objective>
Install two new shadcn UI components (textarea, tabs), create the shared severity tier utility, create the three Zod validation schemas, and establish the test scaffold with passing unit tests. All of these are Wave 1 prerequisites with no dependency on the Prisma schema push (they are pure TypeScript and do not import from @prisma/client).

Purpose: Wave 2 plans (02-03, 02-04, 02-05) import directly from these files. Creating them in Wave 1 allows Wave 2 executor agents to import correct types immediately.

Output: lib/utils/severity.ts, lib/validations/category.ts, lib/validations/product.ts, lib/validations/supplier.ts, components/ui/textarea.tsx, components/ui/tabs.tsx, tests/catalog.test.ts.
</objective>

<execution_context>
@C:\Users\LENOVO\.claude\gsd-core\workflows\execute-plan.md
@C:\Users\LENOVO\.claude\gsd-core\templates\summary.md
</execution_context>

<context>
@C:\Users\LENOVO\logistic-system\.planning\PROJECT.md
@C:\Users\LENOVO\logistic-system\.planning\ROADMAP.md
@C:\Users\LENOVO\logistic-system\.planning\phases\02-catalog\02-CONTEXT.md
@C:\Users\LENOVO\logistic-system\.planning\phases\02-catalog\02-RESEARCH.md
@C:\Users\LENOVO\logistic-system\.planning\phases\02-catalog\02-UI-SPEC.md
@C:\Users\LENOVO\logistic-system\lib\validations\user.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install shadcn components + create severity helper + create Zod schemas</name>
  <files>
    components/ui/textarea.tsx,
    components/ui/tabs.tsx,
    lib/utils/severity.ts,
    lib/validations/category.ts,
    lib/validations/product.ts,
    lib/validations/supplier.ts
  </files>
  <read_first>
    - lib/validations/user.ts — read the exact Zod import path, z.object() structure, z.infer export pattern, and named export convention to replicate for all three new schemas
    - .planning/phases/02-catalog/02-RESEARCH.md — read "Validation (Zod v4 Schemas)" section for exact schema field definitions, "Severity Tier Logic" section for getSeverityBadge implementation, and "New shadcn Components" section for install command
    - .planning/phases/02-catalog/02-UI-SPEC.md — read "Severity Tier Badge Colors" table for exact className strings used in getSeverityBadge return values
    - .planning/phases/02-catalog/02-CONTEXT.md — read D-06 for severity tier conditions (Critical/Warning/OK boundaries)
    - components/ui/ — check which shadcn components already exist to confirm textarea and tabs are absent before installing
  </read_first>
  <behavior>
    - getSeverityBadge(0, 10) → { label: "Critical", className: "bg-red-100 text-red-700 border border-red-200 hover:bg-red-100" }
    - getSeverityBadge(5, 10) → { label: "Warning", className: "bg-amber-100 text-amber-700 border border-amber-200 hover:bg-amber-100" }
    - getSeverityBadge(11, 10) → { label: "OK", className: "bg-green-100 text-green-700 border border-green-200 hover:bg-green-100" }
    - getSeverityBadge(0, 0) → Critical (stock is zero)
    - getSeverityBadge(10, 10) → Warning (equal to threshold, not greater)
    - createProductSchema.safeParse({name:"X",sku:"",categoryId:"abc",reorderThreshold:0}).success === false
    - createProductSchema.safeParse({name:"X",sku:"Y",categoryId:"abc",reorderThreshold:-1}).success === false
    - createProductSchema.safeParse({name:"X",sku:"Y",categoryId:"abc",reorderThreshold:0}).success === true
    - createSupplierSchema.safeParse({name:"S",contactPerson:"P",phone:"123",email:"bad",address:"A"}).success === false
    - createCategorySchema.safeParse({name:""}).success === false
  </behavior>
  <action>
Step 1 — Install shadcn components. Run: npx shadcn@latest add textarea tabs. This creates components/ui/textarea.tsx and components/ui/tabs.tsx. Do NOT modify these generated files — they are managed by shadcn CLI. Do NOT re-run shadcn init (already initialized in Phase 1). Confirm both files exist after install.

Step 2 — Create lib/utils/severity.ts. Export: (a) type SeverityTier = "Critical" | "Warning" | "OK", (b) interface SeverityBadgeProps { label: SeverityTier; className: string }, (c) function getSeverityBadge(currentStock: number, reorderThreshold: number): SeverityBadgeProps. Logic from D-06: if currentStock === 0 return Critical with className "bg-red-100 text-red-700 border border-red-200 hover:bg-red-100"; else if currentStock <= reorderThreshold return Warning with className "bg-amber-100 text-amber-700 border border-amber-200 hover:bg-amber-100"; else return OK with className "bg-green-100 text-green-700 border border-green-200 hover:bg-green-100". No other imports needed — pure TypeScript.

Step 3 — Create lib/validations/category.ts. Import z from "zod". Export: createCategorySchema (z.object with name: z.string().min(1, "Category name is required.").max(100).trim()), updateCategorySchema (adds id: z.string() field), type CreateCategoryInput = z.infer<typeof createCategorySchema>, type UpdateCategoryInput = z.infer<typeof updateCategorySchema>. Follow exact export pattern from lib/validations/user.ts.

Step 4 — Create lib/validations/product.ts. Import z from "zod". Export: createProductSchema (z.object with name: z.string().min(1, "Product name is required."), sku: z.string().min(1, "SKU is required.").max(50).trim(), categoryId: z.string().min(1, "Category is required."), reorderThreshold: z.coerce.number().int().min(0, "Must be 0 or greater")), updateProductSchema (same fields plus id: z.string()), type CreateProductInput, type UpdateProductInput. CRITICAL: do NOT include currentStock in either schema — per D-04/D-05, stock is managed exclusively by Phase 3.

Step 5 — Create lib/validations/supplier.ts. Import z from "zod". Export: createSupplierSchema (z.object with name: z.string().min(1, "Supplier name is required."), contactPerson: z.string().min(1, "Contact person is required."), phone: z.string().min(1, "Phone is required."), email: z.string().email("Please enter a valid email."), address: z.string().min(1, "Address is required.")), updateSupplierSchema (same fields plus id: z.string()), type CreateSupplierInput, type UpdateSupplierInput.

Use z.coerce.number() (not z.number()) for reorderThreshold — FormData.get() returns a string, and coerce handles string-to-number conversion per Pitfall 3 in RESEARCH.md.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - components/ui/textarea.tsx exists and was created by npx shadcn@latest add
    - components/ui/tabs.tsx exists and was created by npx shadcn@latest add
    - lib/utils/severity.ts exports getSeverityBadge, SeverityTier type, and SeverityBadgeProps interface
    - lib/validations/category.ts exports createCategorySchema, updateCategorySchema, CreateCategoryInput, UpdateCategoryInput
    - lib/validations/product.ts exports createProductSchema, updateProductSchema and does NOT export or define any field named currentStock or initialStock
    - lib/validations/supplier.ts exports createSupplierSchema, updateSupplierSchema with all five contact fields required
    - npx tsc --noEmit exits with code 0 (no TypeScript errors in the new files)
  </acceptance_criteria>
  <done>All six files exist with correct exports. TypeScript compiles without errors. getSeverityBadge returns correct labels and classNames per D-06 severity tier rules.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/catalog.test.ts with unit tests and integration stubs</name>
  <files>tests/catalog.test.ts</files>
  <read_first>
    - tests/users.test.ts — read the exact describe/it.todo pattern, comment style, and file structure to replicate for catalog tests
    - tests/vitest.d.ts — confirm the triple-slash reference pattern for vitest globals
    - lib/utils/severity.ts — read the exported function signature to write correct test assertions
    - lib/validations/product.ts — read the schema to know which fields to test
    - lib/validations/supplier.ts — read the schema to know which email validation to test
    - lib/validations/category.ts — read the schema to test name required validation
  </read_first>
  <action>
Create tests/catalog.test.ts. The file structure follows tests/users.test.ts exactly: describe blocks, passing unit tests (not todo), and it.todo stubs for integration tests.

Include a "Severity Tier — lib/utils/severity.ts" describe block with the following passing unit tests (not todo):
- import { getSeverityBadge } from "@/lib/utils/severity" at the top of the file
- Test: getSeverityBadge(0, 10) returns an object with label "Critical"
- Test: getSeverityBadge(5, 10) returns an object with label "Warning"
- Test: getSeverityBadge(11, 10) returns an object with label "OK"
- Test: getSeverityBadge(10, 10) returns an object with label "Warning" (equal to threshold is still Warning)
- Test: getSeverityBadge(0, 0) returns an object with label "Critical" (zero stock is always Critical regardless of threshold)

Include a "Product Validation — lib/validations/product.ts" describe block with passing unit tests:
- import { createProductSchema } from "@/lib/validations/product"
- Test: safeParse with empty sku returns success false
- Test: safeParse with reorderThreshold of -1 returns success false
- Test: safeParse with valid fields (name, sku, categoryId, reorderThreshold 0) returns success true
- Test: reorderThreshold sent as string "5" is coerced to number 5 (validates coerce behavior)

Include a "Supplier Validation — lib/validations/supplier.ts" describe block with passing unit tests:
- import { createSupplierSchema } from "@/lib/validations/supplier"
- Test: safeParse with invalid email returns success false
- Test: safeParse with all required fields including valid email returns success true

Include a "Category Actions — actions/categories.ts" describe block with it.todo stubs for integration tests (matching pattern from users.test.ts). These are stubs only — do NOT attempt to import or mock Server Actions in this plan. Follow the comment format from users.test.ts.

Include a "Product Actions — actions/products.ts" describe block with it.todo stubs: duplicate SKU rejection, toggleProductActive with isActive:false.

The test file must import from "@/lib/utils/severity" and "@/lib/validations/product" using the @/ alias path — vitest.config.ts already configures this alias per [01-04] STATE.md decision.

Do NOT configure a new vitest environment or modify vitest.config.ts — the existing configuration with passWithNoTests: true already handles this test file.
  </action>
  <verify>
    <automated>npm test -- tests/catalog.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - tests/catalog.test.ts imports from "@/lib/utils/severity" and "@/lib/validations/product" and "@/lib/validations/supplier"
    - npm test runs tests/catalog.test.ts and all non-todo tests pass (getSeverityBadge returns correct labels, schema validations return expected success/failure)
    - it.todo stubs for Server Action integration tests exist in the file (following users.test.ts pattern)
    - npm test exits with code 0 (passWithNoTests and todo items do not cause failure)
    - No imports reference @prisma/client in this file — unit tests are pure logic, no DB connection needed
  </acceptance_criteria>
  <done>tests/catalog.test.ts exists with passing unit tests for severity tier logic and Zod schema validation. Integration test stubs are present as it.todo(). npm test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npx shadcn@latest | CLI fetches component definitions from shadcn official registry (ui.shadcn.com) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02-01 | Tampering | lib/validations/product.ts — currentStock field | high | mitigate | createProductSchema and updateProductSchema must NOT include currentStock; acceptance criteria check for absence of the field in schema exports |
| T-02-02-02 | Tampering | shadcn registry — textarea, tabs components | low | accept | Official shadcn registry only (ui.shadcn.com) — no third-party registries; UI-SPEC Registry Safety section confirms PASS |
| T-02-02-03 | Tampering | Zod coerce — reorderThreshold string injection | medium | mitigate | z.coerce.number().int().min(0) rejects non-numeric strings and negative values; unit test in catalog.test.ts verifies boundary |
</threat_model>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` exits with code 0
2. `npm test -- tests/catalog.test.ts` passes with all non-todo tests green
3. `ls components/ui/textarea.tsx components/ui/tabs.tsx` — both files exist
4. `node -e "const {getSeverityBadge}=require('./lib/utils/severity');console.log(getSeverityBadge(0,10).label)"` prints "Critical"
</verification>

<success_criteria>
- getSeverityBadge utility correctly returns Critical/Warning/OK per D-06 boundary conditions
- All three Zod schemas (category, product, supplier) export create + update schemas with correct field types
- Product schema excludes currentStock and initialStock (enforced by test and TypeScript)
- Two new shadcn components available in components/ui/
- Unit tests pass; integration stubs documented as it.todo
</success_criteria>

## Artifacts This Plan Produces

| Artifact | Type | Path | Exported Symbols |
|----------|------|------|-----------------|
| Severity helper | TypeScript | lib/utils/severity.ts | getSeverityBadge, SeverityTier, SeverityBadgeProps |
| Category schema | TypeScript | lib/validations/category.ts | createCategorySchema, updateCategorySchema, CreateCategoryInput, UpdateCategoryInput |
| Product schema | TypeScript | lib/validations/product.ts | createProductSchema, updateProductSchema, CreateProductInput, UpdateProductInput |
| Supplier schema | TypeScript | lib/validations/supplier.ts | createSupplierSchema, updateSupplierSchema, CreateSupplierInput, UpdateSupplierInput |
| Textarea component | TSX | components/ui/textarea.tsx | Textarea (shadcn managed) |
| Tabs component | TSX | components/ui/tabs.tsx | Tabs, TabsList, TabsTrigger, TabsContent (shadcn managed) |
| Test scaffold | TypeScript | tests/catalog.test.ts | unit tests + it.todo stubs |

<output>
Create `.planning/phases/02-catalog/02-02-SUMMARY.md` when done
</output>
