---
phase: 01-foundation
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - package.json
  - vitest.config.ts
  - tests/setup.ts
  - tests/auth.test.ts
  - tests/middleware.test.ts
  - tests/sidebar.test.tsx
  - tests/users.test.ts
  - tests/validations.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "`npx vitest run` completes with exit code 0 — no runner errors, all stubs reported as todo/pending (AUTH-01, AUTH-02, AUTH-03)"
    - "Every requirement row in VALIDATION.md has a corresponding it.todo stub in the matching test file"
    - "Test framework is Vitest with jsdom environment; `npx vitest run` takes under 15 seconds"
  artifacts:
    - "vitest.config.ts — Vitest config with jsdom environment, globals: true, setupFiles reference"
    - "tests/setup.ts — Vitest setup file (cleanup hooks)"
    - "tests/auth.test.ts — Stubs for AUTH-01 (valid login, invalid login, deactivated user) and AUTH-02 (session persistence)"
    - "tests/middleware.test.ts — Stubs for AUTH-03 (STAFF on /dashboard → redirect, unauthenticated → redirect, MANAGER → allowed)"
    - "tests/sidebar.test.tsx — Stubs for D-06 (MANAGER 9 items) and D-07 (STAFF 6 items)"
    - "tests/users.test.ts — Stubs for D-03 (Manager creates user) and D-05 (wrong current password rejected)"
    - "tests/validations.test.ts — Stubs for D-05 (password mismatch → Zod error)"
  key_links:
    - "vitest.config.ts must declare `environment: 'jsdom'` for sidebar.test.tsx to work (uses DOM APIs)"
    - "package.json test script must be `vitest run` (not `vitest`) — CI mode, not watch mode"
    - "tests/setup.ts is registered in vitest.config.ts setupFiles array"
---

<objective>
Install Vitest and create the Wave 0 test stub files that map to every requirement in
01-VALIDATION.md. Stubs use `it.todo` so the test runner exits cleanly without failures,
establishing the contract for test implementations in Plans 01-04 and future phases.

Purpose: The Nyquist validation rule requires automated verify commands on every task.
These test files must exist before implementation so that `npx vitest run tests/X.test.ts`
is a valid command in Plans 01-04 verify blocks.

Output: Vitest installed; 5 test files with todo stubs covering all 12 test cases from
VALIDATION.md; `npx vitest run` exits 0.
</objective>

<execution_context>
@C:/Users/LENOVO/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/LENOVO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest + Create Test Infrastructure</name>
  <files>
    package.json,
    vitest.config.ts,
    tests/setup.ts
  </files>
  <read_first>
    .planning/phases/01-foundation/01-VALIDATION.md — Test Infrastructure section (framework: Vitest, config file: vitest.config.ts, quick run command), Sampling Rate section, Wave 0 Requirements checklist
    .planning/phases/01-foundation/01-RESEARCH.md — Validation Architecture section (npm install command for Vitest, vitest.config.ts shape)
  </read_first>
  <action>
    STEP 1 — Install Vitest and testing utilities (must run AFTER Plan 01-01 has created package.json):
    Run `npm install -D vitest @vitejs/plugin-react @testing-library/react @testing-library/dom jsdom`.
    These are devDependencies only — never install in production dependencies.

    STEP 2 — Create vitest.config.ts at project root:
    Import defineConfig from "vitest/config" and react from "@vitejs/plugin-react".
    Export default defineConfig with:
      plugins: [react()],
      test object with:
        environment: "jsdom" (required for sidebar.test.tsx which uses DOM APIs via usePathname)
        globals: true (enables describe/it/expect without imports in test files)
        setupFiles: ["tests/setup.ts"]
        include: ["tests/**/*.{test,spec}.{ts,tsx}"]

    STEP 3 — Create tests/setup.ts:
    Import afterEach from "vitest"; import cleanup from "@testing-library/react".
    Call afterEach(cleanup) to reset DOM between tests.
    This file is minimal — it only handles cleanup. No global mocks here; mocks are per-test.

    STEP 4 — Update package.json scripts to add test commands:
    Add "test": "vitest run" (CI mode, exits after run, no watch)
    Add "test:watch": "vitest" (watch mode for development)
    Add "test:coverage": "vitest run --coverage"
    NOTE: The "test" script must be "vitest run" not "vitest" — watch mode hangs in CI.
  </action>
  <verify>
    <automated>npx vitest run --reporter=dot 2>&1; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 (even with zero test files — Vitest exits cleanly when no files match the pattern)
    - vitest.config.ts exists with environment: "jsdom" and globals: true
    - tests/setup.ts exists with afterEach(cleanup) import
    - package.json "test" script is "vitest run" (not "vitest" which would hang in watch mode)
    - Vitest, @testing-library/react, jsdom are in devDependencies
    - @types/bcryptjs is NOT newly added (verify: grep package.json for @types/bcryptjs should return nothing)
  </acceptance_criteria>
  <done>Vitest installed and configured; jsdom environment enabled; test infrastructure ready for stub files</done>
</task>

<task type="auto">
  <name>Task 2: Create Test Stub Files (Wave 0 Coverage)</name>
  <files>
    tests/auth.test.ts,
    tests/middleware.test.ts,
    tests/sidebar.test.tsx,
    tests/users.test.ts,
    tests/validations.test.ts
  </files>
  <read_first>
    .planning/phases/01-foundation/01-VALIDATION.md — Per-Task Verification Map table (all 12 rows: task ID, requirement, secure behavior, test type, automated command)
    .planning/phases/01-foundation/01-RESEARCH.md — Validation Architecture → Phase Requirements to Test Map
    .planning/phases/01-foundation/01-CONTEXT.md — D-03 (no self-registration), D-05 (password change), D-06 (MANAGER 9 items), D-07 (STAFF 6 items)
  </read_first>
  <action>
    Create 5 test stub files. Each file uses describe() blocks with it.todo() stubs matching the
    VALIDATION.md per-task verification map. Using it.todo() (not it.skip()) — todo stubs are
    semantically correct for "not yet implemented" and are reported distinctly by Vitest.
    Each stub's description string must exactly match the "Secure Behavior" column from VALIDATION.md
    so that future implementers can trace test → requirement unambiguously.

    FILE: tests/auth.test.ts
    Purpose: AUTH-01 (valid login, invalid login, deactivated user), AUTH-02 (session persistence).
    Structure: One top-level describe "Authentication — actions/auth.ts" containing:
      it.todo for "valid credentials return a session and redirect to role home page" (task 01-auth-01, AUTH-01, T-1-01)
      it.todo for "invalid credentials return an error message without throwing to the client" (task 01-auth-02, AUTH-01, T-1-01)
      it.todo for "deactivated user cannot log in even with correct password" (task 01-auth-03, AUTH-01, T-1-01)
      it.todo for "JWT session cookie persists across a simulated browser refresh" (task 01-auth-04, AUTH-02)
    Implementation notes (in comments above each stub): These will require mocking prisma.user.findUnique
    and bcryptjs.compare with vi.mock(); each test calls the authorize function extracted from lib/auth.ts.

    FILE: tests/middleware.test.ts
    Purpose: AUTH-03 RBAC enforcement by middleware.ts.
    Structure: One top-level describe "Middleware RBAC Guard — middleware.ts" containing:
      it.todo for "STAFF accessing /dashboard is redirected to /inventory silently" (task 01-rbac-01, AUTH-03, T-1-02)
      it.todo for "unauthenticated request to /inventory is redirected to /login" (task 01-rbac-02, AUTH-03, T-1-02)
      it.todo for "MANAGER accessing /dashboard is allowed through" (task 01-rbac-03, AUTH-03, T-1-02)
    Implementation notes: These will require creating a mock NextRequest with a mocked auth() return.

    FILE: tests/sidebar.test.tsx
    Purpose: D-06 (MANAGER sidebar 9 items), D-07 (STAFF sidebar 6 items absent items not hidden).
    Structure: One top-level describe "Sidebar — components/sidebar.tsx" containing:
      it.todo for "MANAGER role renders exactly 9 navigation items" (task 01-sidebar-01, AUTH-03)
      it.todo for "STAFF role renders exactly 6 navigation items with Dashboard, Reports, and Users absent" (task 01-sidebar-02, AUTH-03)
    Implementation notes: These will render the Sidebar component with role="MANAGER" or role="STAFF"
    using @testing-library/react render(); query all nav links and assert count.
    Note: Sidebar is a Server Component — tests will need to render it as a regular function call
    or extract the nav item filtering logic into a pure testable function.

    FILE: tests/users.test.ts
    Purpose: D-03 (Manager creates user, user in DB), D-05 (wrong current password rejected).
    Structure: One top-level describe "User Management — actions/users.ts" containing:
      it.todo for "Manager creating a user with valid data results in the user being saved to the database" (task 01-users-01, AUTH-03)
      it.todo for "Attempting password change with wrong current password returns an error" (task 01-users-03, AUTH-03)
    Implementation notes: These will require mocking prisma.user.create/findUnique and bcryptjs.compare.

    FILE: tests/validations.test.ts
    Purpose: D-05 Zod schema validation (password mismatch).
    Structure: One top-level describe "Zod Validation Schemas — lib/validations/user.ts" containing:
      it.todo for "changePasswordSchema rejects when newPassword and confirmPassword do not match" (task 01-users-02, AUTH-03)
    Implementation notes: This is a pure unit test — import changePasswordSchema and call .safeParse()
    with mismatched passwords; assert result.success is false and error path includes confirmPassword.
    No mocking required.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 with no test failures (all stubs are todo — not failing)
    - tests/auth.test.ts contains 4 it.todo stubs for AUTH-01 and AUTH-02
    - tests/middleware.test.ts contains 3 it.todo stubs for AUTH-03
    - tests/sidebar.test.tsx contains 2 it.todo stubs for D-06 and D-07
    - tests/users.test.ts contains 2 it.todo stubs for D-03 and D-05
    - tests/validations.test.ts contains 1 it.todo stub for D-05 (password mismatch)
    - Total: 12 todo stubs covering all 12 rows in VALIDATION.md Per-Task Verification Map
    - `npx vitest run --reporter=verbose` output shows "12 todos" (or similar todo count) with 0 failures
  </acceptance_criteria>
  <done>12 test stubs created across 5 files covering all VALIDATION.md requirement rows; npx vitest run exits 0; Wave 0 test coverage contract established</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test files → production code | Tests import from src — no production secrets should be in test files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-TST | Information Disclosure | tests/ directory containing hardcoded credentials | low | accept | Test files use mock credentials (e.g., "test@example.com", "TestPass@123") — never production DATABASE_URL or AUTH_SECRET. Vitest runs against mocked Prisma, not a live database. |
| T-1-SC | Tampering | npm install of Vitest ecosystem packages | medium | mitigate | Package Legitimacy Audit in 01-RESEARCH.md covers bcryptjs, prisma, next-auth. Vitest + @testing-library packages are widely used (100M+ weekly downloads combined); treated as approved. No slopcheck checkpoint required. |
</threat_model>

## Artifacts This Phase Produces

| Artifact | Path | Consumer |
|----------|------|---------|
| Vitest config | vitest.config.ts | `npm test`, `npx vitest run` |
| Test setup | tests/setup.ts | vitest.config.ts setupFiles |
| Auth test stubs | tests/auth.test.ts | Plan 01-04 fills in createUser/changePassword tests |
| Middleware test stubs | tests/middleware.test.ts | Filled in during Phase 1 verification |
| Sidebar test stubs | tests/sidebar.test.tsx | Plan 01-04 (filled after sidebar component is complete) |
| User action test stubs | tests/users.test.ts | Plan 01-04 fills these in |
| Validation test stubs | tests/validations.test.ts | Plan 01-04 fills the password schema test |

<verification>
Run after both tasks complete:

1. `npx vitest run --reporter=verbose` — exits 0; displays 12 todo stubs, 0 failures
2. `npx tsc --noEmit` — still zero TypeScript errors (vitest.config.ts and test files do not break compilation)
3. Confirm `npm test` is now a valid command: `npm test -- --reporter=dot`
</verification>

<success_criteria>
- Nyquist compliance: every requirement row in VALIDATION.md (12 rows) has a corresponding `it.todo` stub
- `npx vitest run` exits 0 — test infrastructure is healthy before any implementation tests are written
- All stub descriptions precisely match the "Secure Behavior" from VALIDATION.md for traceability
- package.json "test" script runs in CI mode (no watch)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done.
</output>
