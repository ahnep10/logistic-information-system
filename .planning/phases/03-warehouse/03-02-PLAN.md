---
phase: 03-warehouse
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/(protected)/stock/page.tsx
  - app/(protected)/stock/stock-client.tsx
autonomous: true
requirements:
  - INVT-01
  - INVT-02
  - INVT-04
  - INVT-06

must_haves:
  truths:
    - "Staff and Manager can open the Record Stock In dialog from /stock, fill product/quantity/reason, submit, and see the new row appear at the top of the Recent Transactions table (INVT-01)"
    - "Staff and Manager can open the Record Stock Out dialog, submit a quantity that exceeds current stock, and see the inline error message with the current stock count — dialog stays open, form is not reset (INVT-02/D-18)"
    - "After any successful stock mutation, the /products page severity badges update on next visit because revalidatePath('/products') is called in the Server Action (INVT-04/INVT-06)"
    - "Recent Transactions table shows last 10 transactions newest first with columns: Date/Time, Product, Type badge (IN green/OUT red soft tint), Qty, Reason, Recorded By"
    - "Both dialogs use the base-ui render prop pattern for DialogTrigger (render={<Button>...}) — NOT asChild"
    - "Product dropdown in both dialogs shows only isActive:true products (fetched server-side)"
  artifacts:
    - app/(protected)/stock/page.tsx (replaces 3-line stub)
    - app/(protected)/stock/stock-client.tsx (new file)
  key_links:
    - "StockClient receives recentTransactions and products props from page.tsx server fetch"
    - "RecordStockInDialog calls recordStockIn(fd) from actions/stock-transactions.ts"
    - "RecordStockOutDialog calls recordStockOut(fd) from actions/stock-transactions.ts"
    - "Type badge className: STOCK_IN uses bg-green-100 text-green-700 border border-green-200; STOCK_OUT uses bg-red-100 text-red-700 border border-red-200 per UI-SPEC (NOT Badge variant='destructive')"
---

## Phase Goal

**As a** warehouse staff member, **I want to** record stock in and stock out movements and view full transaction history with date and product filters, **so that** inventory levels stay accurate in real time and low-stock products are automatically flagged without manual intervention.

<objective>
Deliver the /stock page as a complete vertical slice: warehouse staff can record stock-in and stock-out transactions through dialogs, and immediately see the result in the Recent Transactions table. This plan replaces the stub /stock page with a fully functional server component and creates the stock-client.tsx with two dialog forms.

Purpose: After this plan, the core stock mutation loop is end-to-end functional: user opens dialog → fills form → submits → Server Action runs atomic DB transaction → page revalidates → updated table visible. INVT-01, INVT-02, INVT-04, and INVT-06 are satisfied at the UI + mutation layer.

Output: app/(protected)/stock/page.tsx (server component), app/(protected)/stock/stock-client.tsx (client component with two dialogs and recent transactions table).
</objective>

<execution_context>
@C:\Users\LENOVO\.claude\gsd-core\workflows\execute-plan.md
@C:\Users\LENOVO\.claude\gsd-core\templates\summary.md
</execution_context>

<context>
@C:\Users\LENOVO\logistic-system\.planning\PROJECT.md
@C:\Users\LENOVO\logistic-system\.planning\ROADMAP.md
@C:\Users\LENOVO\logistic-system\.planning\phases\03-warehouse\03-CONTEXT.md
@C:\Users\LENOVO\logistic-system\.planning\phases\03-warehouse\03-RESEARCH.md
@C:\Users\LENOVO\logistic-system\.planning\phases\03-warehouse\03-UI-SPEC.md

# Pattern references — read before implementing
@C:\Users\LENOVO\logistic-system\app\(protected)\suppliers\suppliers-client.tsx
@C:\Users\LENOVO\logistic-system\app\(protected)\products\page.tsx
@C:\Users\LENOVO\logistic-system\actions\stock-transactions.ts
@C:\Users\LENOVO\logistic-system\lib\validations\stock-transaction.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app/(protected)/stock/stock-client.tsx with RecordStockInDialog, RecordStockOutDialog, and RecentTransactionsTable</name>
  <files>app/(protected)/stock/stock-client.tsx</files>
  <read_first>
    - app/(protected)/suppliers/suppliers-client.tsx — read the full file: "use client" directive, import structure (useState, useForm, zodResolver, lucide icons, shadcn components, actions), Dialog open/close state pattern (lines ~193–213), DialogTrigger render prop usage (lines ~217, ~297), DialogClose render prop usage, server error state pattern (lines ~293–295), FormField with Select (lines ~48–54), onSubmit FormData build pattern (Object.entries forEach)
    - actions/stock-transactions.ts — read both exports (recordStockIn, recordStockOut) to import correctly and understand the return shape { success: true } | { error: string }
    - lib/validations/stock-transaction.ts — read all exports: stockInSchema, stockOutSchema, StockInInput, StockOutInput for useForm generics and zodResolver
    - .planning/phases/03-warehouse/03-UI-SPEC.md — read "Screen 1: Stock Transactions Page" section for exact layout, table column specs, dialog field specs, type badge className values, copywriting strings, empty state spec, and the critical note that type badges use className NOT variant="destructive"
    - .planning/phases/03-warehouse/03-RESEARCH.md — read the "State of the Art" section for base-ui render prop pattern (render={} not asChild) and Zod 4 compatibility note
    - app/(protected)/stock/page.tsx — read the current 3-line stub to confirm the file to replace in Task 2 (not this task)
  </read_first>
  <action>
Create app/(protected)/stock/stock-client.tsx as a new file.

LINE 1: "use client"

IMPORTS: import useState from "react"; import useForm from "react-hook-form"; import zodResolver from "@hookform/resolvers/zod"; import { Loader2, PackagePlus, PackageMinus, ArrowLeftRight } from "lucide-react"; import Button from "@/components/ui/button"; import Badge from "@/components/ui/badge"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table"; import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger, DialogFooter, DialogClose } from "@/components/ui/dialog"; import { Form, FormControl, FormField, FormItem, FormLabel, FormMessage } from "@/components/ui/form"; import { Input } from "@/components/ui/input"; import { Textarea } from "@/components/ui/textarea"; import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"; import { stockInSchema, stockOutSchema, type StockInInput, type StockOutInput } from "@/lib/validations/stock-transaction"; import { recordStockIn, recordStockOut } from "@/actions/stock-transactions"

TYPES: Define a Product interface with id: string, name: string, sku: string. Define a RecentTransaction interface with id: string, type: "STOCK_IN" | "STOCK_OUT", quantity: number, reason: string, createdAt: Date, product: { name: string, sku: string }, createdBy: { name: string | null }. Define StockClientProps: { recentTransactions: RecentTransaction[], products: Product[] }.

TYPE BADGE HELPER: Define a getTypeBadgeClass function that returns "bg-green-100 text-green-700 border border-green-200 hover:bg-green-100" for STOCK_IN and "bg-red-100 text-red-700 border border-red-200 hover:bg-red-100" for STOCK_OUT. Do NOT use Badge variant="destructive" for OUT per UI-SPEC.

DATE FORMATTER: Define a formatDateTime function that uses new Intl.DateTimeFormat("en-US", { month: "short", day: "numeric", year: "numeric", hour: "2-digit", minute: "2-digit" }).format(new Date(date)) to produce the format "Jul 1, 2026, 14:32".

EXPORT DEFAULT StockClient({ recentTransactions, products }):
Render a div wrapper. Top section has a page header: flex items-center justify-between mb-6 containing an h1 with text "Record Stock" (text-2xl font-semibold). Below the header, an action bar with flex gap-3 mb-8 containing two buttons side by side: RecordStockInDialog and RecordStockOutDialog trigger elements. Below the action bar, a Card containing the recent transactions table.

RECORDSTOCKINDIALOG function component (takes { products }):
- useState for open (boolean) and serverError (string | null)
- useForm<StockInInput> with zodResolver(stockInSchema) and defaultValues: { productId: "", quantity: 1, reason: "Purchase Received", notes: "" }
- onSubmit async: setServerError(null), build FormData via Object.entries(values).forEach(([k, v]) => fd.append(k, String(v ?? ""))), call result = await recordStockIn(fd), if result has error property set serverError and return, else form.reset() and setOpen(false)
- Dialog with open={open} onOpenChange={setOpen}
- DialogTrigger with render prop: render={<Button variant="default"><PackagePlus className="h-4 w-4 mr-2" />Record Stock In</Button>} — this is the BASE-UI render prop pattern, NOT asChild
- DialogContent max-w-md with DialogHeader containing DialogTitle "Record Stock In"
- Form with four FormFields:
  1. productId — FormLabel "Product", FormControl contains Select with onValueChange={field.onChange} defaultValue={field.value}, SelectTrigger + SelectValue placeholder "Select a product", SelectContent mapping products array to SelectItem key={p.id} value={p.id} text "{p.name} ({p.sku})"
  2. quantity — FormLabel "Quantity", FormControl contains Input type="number" min={1} placeholder="Enter quantity"
  3. reason — FormLabel "Reason", FormControl contains Select with options "Purchase Received", "Return", "Manual Adjustment" each as SelectItem. Placeholder "Select a reason"
  4. notes — FormLabel "Notes", FormControl contains Textarea placeholder="Optional notes" rows={3}
- Below the last FormField but inside Form, conditionally render: {serverError && <p className="text-sm text-destructive">{serverError}</p>}
- DialogFooter: DialogClose with render prop render={<Button type="button" variant="outline">Discard</Button>} and a submit Button showing {form.formState.isSubmitting ? (<><Loader2 className="mr-2 h-4 w-4 animate-spin" />Recording...</>) : "Record Stock In"} with disabled={form.formState.isSubmitting}

RECORDSTOCKOUTDIALOG function component (takes { products }):
- Same structure as RecordStockInDialog with these differences:
- useForm<StockOutInput> with zodResolver(stockOutSchema) and defaultValues: { productId: "", quantity: 1, reason: "Sale", notes: "" }
- onSubmit calls recordStockOut instead of recordStockIn
- DialogTitle is "Record Stock Out"
- DialogTrigger render prop uses PackageMinus icon and text "Record Stock Out"
- reason FormField options are "Sale", "Manual Adjustment", "Write-Off"
- The serverError display for insufficient-stock: render {serverError && <p className="text-sm text-destructive">{serverError}</p>} above the DialogFooter — dialog stays open, form is NOT reset on error per D-18
- Submit button label "Record Stock Out"

RECENT TRANSACTIONS TABLE (rendered inside Card in StockClient):
CardHeader with CardTitle "Recent Transactions" (text-base font-semibold). CardContent with p-0. Table with TableHeader row: columns Date / Time (w-40), Product (auto), Type (w-[72px]), Qty (w-16 text-right), Reason (w-40), Recorded By (w-32). TableBody: when recentTransactions.length is 0, render a single TableRow with TableCell colSpan={6} containing centered flex div (flex flex-col items-center py-12 text-center) with ArrowLeftRight icon (w-8 h-8 text-muted-foreground/30 mb-3), p "No transactions yet" (text-sm font-medium), p "Record a stock movement to see it here." (text-sm text-muted-foreground mt-1). When populated, map each transaction to a TableRow: Date/Time cell using formatDateTime(tx.createdAt), Product cell with tx.product.name (text-sm font-medium), Type cell with Badge className={getTypeBadgeClass(tx.type)} and label {tx.type === "STOCK_IN" ? "IN" : "OUT"}, Qty cell with tx.quantity (text-right text-sm font-semibold), Reason cell with tx.reason (text-sm text-muted-foreground), Recorded By cell with tx.createdBy.name ?? "—" (text-sm text-muted-foreground).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - app/(protected)/stock/stock-client.tsx starts with "use client" on line 1
    - File exports default function StockClient with recentTransactions and products props
    - RecordStockInDialog and RecordStockOutDialog are defined as separate function components
    - Both dialog trigger buttons use DialogTrigger with render prop pattern (render={<Button>...}) — no asChild prop anywhere in the file
    - Both dialog close buttons use DialogClose with render prop pattern (render={<Button type="button" variant="outline">Discard</Button>})
    - The getTypeBadgeClass helper returns the soft-tint className strings (bg-green-100 / bg-red-100) — Badge variant="destructive" is NOT used anywhere in the file
    - RecordStockOutDialog displays serverError inside the dialog content (not as toast, not as page-level error) and does NOT call form.reset() on error
    - Empty state for recent transactions table renders ArrowLeftRight icon and "No transactions yet" heading
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>stock-client.tsx created with two dialog forms (Stock In / Stock Out) and recent transactions table. Both dialogs use base-ui render prop pattern. Type badges use className approach not variant prop. Insufficient-stock error displays inline in Stock Out dialog with form staying open.</done>
</task>

<task type="auto">
  <name>Task 2: Replace app/(protected)/stock/page.tsx stub with async server component</name>
  <files>app/(protected)/stock/page.tsx</files>
  <read_first>
    - app/(protected)/stock/page.tsx — read the current 3-line stub to understand what is being replaced
    - app/(protected)/products/page.tsx — read the full file: async function pattern, Promise.all for parallel Prisma fetches, data mapping before passing to client component, import pattern for prisma from "@/lib/prisma"
    - app/(protected)/stock/stock-client.tsx — read the StockClientProps interface to confirm exact prop names (recentTransactions, products) and field shapes before writing the page
    - .planning/phases/03-warehouse/03-CONTEXT.md — read D-11 (last 10 transactions, newest first), D-12 (no auth() call in page — session checked in Server Actions)
    - .planning/phases/03-warehouse/03-RESEARCH.md — read the "Recommended Project Structure" section and Pattern 5 (multiple revalidatePath) to confirm what data the page must fetch
  </read_first>
  <action>
Fully replace app/(protected)/stock/page.tsx. The current content (3-line h1 stub) is discarded entirely.

Imports: import { prisma } from "@/lib/prisma"; import StockClient from "./stock-client"

Do NOT import auth() in the page. The page is protected by middleware. Stock transaction Server Actions check the session themselves per D-12. No isManager prop needed — both Staff and Manager see identical UI per D-12.

Export default async function StockPage().

Inside: use Promise.all to fetch both data sets in parallel:
- prisma.stockTransaction.findMany with orderBy: { createdAt: "desc" }, take: 10, and include: { product: { select: { id: true, name: true, sku: true } }, createdBy: { select: { name: true } } } — this satisfies D-11 (last 10 transactions, newest first)
- prisma.product.findMany with where: { isActive: true }, orderBy: { name: "asc" }, select: { id: true, name: true, sku: true } — only active products per D-09/D-10

Return: JSX rendering StockClient with props recentTransactions={recentTransactions} and products={activeProducts}.

The page does NOT include a Suspense boundary (no useSearchParams consumer in stock-client.tsx). StockClient is a pure server-prop consumer.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npm test</automated>
  </verify>
  <acceptance_criteria>
    - app/(protected)/stock/page.tsx is an async server component (no "use client" directive, no useState)
    - Page uses Promise.all with two Prisma queries (stockTransaction.findMany take:10 + product.findMany isActive:true)
    - stockTransaction.findMany uses include: { product: { select: ... }, createdBy: { select: ... } }
    - No auth() import or call in page.tsx (session checking is done inside Server Actions per D-12)
    - Return statement renders StockClient with recentTransactions and products props
    - npm test exits with code 0 (no regression)
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>app/(protected)/stock/page.tsx is a functional async server component that fetches the last 10 transactions and all active products in parallel, then passes them to StockClient. The /stock route is fully functional end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form → Server Action | recordStockIn / recordStockOut receive FormData from client; all values are untrusted |
| Server Action → DB | prisma.$transaction wraps product update + transaction create; session.user.id is the FK for audit trail |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering | RecordStockOutDialog — productId IDOR (user passes arbitrary product ID) | high | mitigate | Server Action verifies product exists via tx.$queryRaw SELECT FOR UPDATE inside transaction; non-existent product throws "Product not found." and rolls back |
| T-03-06 | Tampering | RecordStockInDialog / RecordStockOutDialog — quantity as string bypasses int check | medium | mitigate | stockInSchema / stockOutSchema use z.preprocess with Number() coercion then z.number().int().min(1); string "abc" produces NaN which fails z.number() |
| T-03-07 | Tampering | StockClient — products dropdown shows deactivated products | medium | mitigate | page.tsx fetches products with where: { isActive: true }; deactivated products never appear in dropdown per D-09/D-10 |
| T-03-08 | Information Disclosure | Recent Transactions table — shows other users' transactions | low | accept | All authenticated users can view transactions per D-12 requirement; this is intended behavior (warehouse staff all share visibility) |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No new package installs this plan |
</threat_model>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` exits with code 0
2. `npm test` exits with code 0
3. Navigate to /stock — page renders "Record Stock" heading, two side-by-side buttons (Record Stock In / Record Stock Out), and the Recent Transactions card
4. Click "Record Stock In" — dialog opens with Product select, Quantity input, Reason select, Notes textarea
5. Submit a valid Stock In — dialog closes, new row appears at top of Recent Transactions table with green "IN" badge
6. Click "Record Stock Out" — try quantity > current stock — inline error "Insufficient stock. Current stock: N units." appears inside dialog; dialog stays open
7. Navigate to /products — severity badges show updated stock levels
</verification>

<success_criteria>
- /stock page renders correctly for both Manager and Staff roles
- Stock In and Stock Out dialogs submit successfully with correct data
- Recent Transactions table shows last 10 transactions with correct columns and type badges
- Insufficient-stock error displays inline in Stock Out dialog without closing it
- Both dialogs use base-ui render prop pattern throughout
- No TypeScript errors; no test regressions
</success_criteria>

## Artifacts This Plan Produces

| Artifact | Type | Path | Exported Symbols |
|----------|------|------|-----------------|
| Stock page | TSX Server Component | app/(protected)/stock/page.tsx | StockPage (default) |
| Stock client component | TSX Client Component | app/(protected)/stock/stock-client.tsx | StockClient (default), RecordStockInDialog, RecordStockOutDialog, getTypeBadgeClass, formatDateTime |

<output>
Create `.planning/phases/03-warehouse/03-02-SUMMARY.md` when done
</output>
