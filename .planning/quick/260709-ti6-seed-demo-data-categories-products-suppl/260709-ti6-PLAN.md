---
phase: quick-260709-ti6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/dummy-data.ts
  - package.json
  - app/(protected)/dashboard/page.tsx
  - app/(protected)/dashboard/dashboard-client.tsx
  - tests/dashboard.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Running `npm run db:dummy` twice in a row completes both times with exit code 0 (idempotent full-wipe-then-reseed), leaving the 2 pre-existing users (admin@logistics.com MANAGER, karin@logis.com STAFF) untouched, and prints a final summary of the current row counts for Category/Product/Supplier/User/StockTransaction/PurchaseOrder"
    - "No Product ever has currentStock < 0 — every seeded product's currentStock equals the net result of its own StockTransaction history (natural movements plus any linked PO-received quantities), computed via a clamped running-balance so no STOCK_OUT is ever applied for more than is on hand at that point"
    - "Severity tiers (lib/utils/severity.ts) are visibly mixed across seeded products: at least 2 at Critical (currentStock === 0), at least 4 at Warning (0 < currentStock <= reorderThreshold), remainder at OK"
    - "Every RECEIVED PurchaseOrder's line items have receivedQuantity populated and a linked StockTransaction (type STOCK_IN, reason 'Purchase Received', purchaseOrderId set) exists for each, mirroring actions/purchase-orders.ts's receivePurchaseOrder"
    - "Visiting /dashboard shows a new 'Produk Paling Sering Keluar' Card with a Recharts BarChart ranking products by total STOCK_OUT quantity, positioned directly below the existing Purchase Order Status Card, with an empty-state matching the existing pattern when no STOCK_OUT data exists — and the existing KPI tiles grid + PO status PieChart are visually and functionally unchanged"
    - "`npx tsc --noEmit` and `npm test` (vitest run, full suite) both exit 0 with zero regressions"
  artifacts:
    - path: "prisma/dummy-data.ts"
      provides: "Standalone full-wipe + reseed script (categories, products, suppliers, 2 new staff users, stock transactions, purchase orders) — separate from prisma/seed.ts, which is not modified"
    - path: "package.json"
      provides: "\"db:dummy\": \"tsx prisma/dummy-data.ts\" npm script"
    - path: "app/(protected)/dashboard/page.tsx"
      provides: "topSellingProducts server query (stockTransaction.groupBy + product.findMany) passed as a new prop to DashboardClient"
    - path: "app/(protected)/dashboard/dashboard-client.tsx"
      provides: "New 'Produk Paling Sering Keluar' Card + Recharts BarChart section, additive only"
    - path: "tests/dashboard.test.ts"
      provides: "Updated prisma mock (stockTransaction.groupBy, product.findMany) + new test covering the topSellingProducts mapping"
  key_links:
    - from: "prisma/dummy-data.ts's per-product movement generation"
      to: "Product.currentStock"
      via: "A clamped running-balance helper (applyMovements-style: sort by daysAgo descending, add STOCK_IN unconditionally, clamp STOCK_OUT to min(quantity, running)) guarantees the DB's currentStock >= 0 CHECK constraint is never violated"
      pattern: "running balance clamp, mirrored from the row-lock intent (never let stock go negative) already used in actions/stock-transactions.ts's recordStockOut"
    - from: "prisma.stockTransaction.groupBy (STOCK_OUT, by productId, take 6)"
      to: "DashboardClient's topSellingProducts prop"
      via: "page.tsx maps groupBy results through a prisma.product.findMany name lookup, same two-step aggregate-then-lookup shape as the existing poStatusGroups -> fillPoStatusCounts pattern"
      pattern: "groupBy + findMany + map, no Decimal/BigInt crossing the server->client boundary (StockTransaction.quantity is Int)"
    - from: "actions/purchase-orders.ts's receivePurchaseOrder"
      to: "prisma/dummy-data.ts's RECEIVED-PO seeding"
      via: "Each RECEIVED line item gets receivedQuantity populated and a linked STOCK_IN StockTransaction with purchaseOrderId set, exactly mirroring the production side-effect (minus the row lock, unnecessary in a single-threaded script)"
      pattern: "type: STOCK_IN, reason: 'Purchase Received', purchaseOrderId set"
---

<objective>
Seed realistic Indonesian-market demo data (categories, products, suppliers, 2 additional staff users, stock transactions, purchase orders) via a new idempotent `prisma/dummy-data.ts` script after a full wipe of the junk/test data currently in the dev DB, then add an additive "Top Selling Products" Recharts bar chart to the Dashboard sourced from that data.

Purpose: The current dev DB only contains obvious QA test artifacts (3 products all named "Samsong A56", a placeholder supplier "aery"). Real-looking, internally-consistent demo data is needed to meaningfully demo/screenshot the app and to give the new dashboard chart something non-trivial to rank.
Output: `prisma/dummy-data.ts` (new, wipes + reseeds every table except `users`, only adding 2 new staff via upsert), a `db:dummy` npm script, and an additive "Produk Paling Sering Keluar" BarChart on `/dashboard` built from a new Prisma groupBy query — with zero changes to any other existing dashboard markup/logic.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@prisma/schema.prisma
@prisma/seed.ts
@actions/stock-transactions.ts
@actions/purchase-orders.ts
@lib/validations/stock-transaction.ts
@lib/validations/purchase-order.ts
@lib/validations/product.ts
@lib/utils/severity.ts
@lib/utils/dashboard.ts
@app/(protected)/dashboard/page.tsx
@app/(protected)/dashboard/dashboard-client.tsx
@tests/dashboard.test.ts
@package.json

<interfaces>
`prisma/seed.ts` is the pattern to mirror structurally for `prisma/dummy-data.ts` (own `new PrismaClient()`, `main().catch(...).finally(() => prisma.$disconnect())`, `bcryptjs`'s `hash(password, 12)` convention) — but it must NOT be imported from or edited; `dummy-data.ts` is a fully separate script.

`actions/stock-transactions.ts`'s `recordStockIn`/`recordStockOut` establish the only two `TransactionType` values (`STOCK_IN`/`STOCK_OUT`) and the invariant that a STOCK_OUT can never exceed current stock. `lib/validations/stock-transaction.ts` fixes the exact `reason` string literals to reuse for realism: STOCK_IN = `"Purchase Received" | "Return" | "Manual Adjustment"`, STOCK_OUT = `"Sale" | "Manual Adjustment" | "Write-Off"`.

`actions/purchase-orders.ts`'s `computeTotalAmount` sums `new Prisma.Decimal(quantity).times(unitPrice)` across line items into a `Prisma.Decimal` — reuse this exact formula for each seeded PO's `totalAmount` (`import { Prisma } from "@prisma/client"`). Its `receivePurchaseOrder` shows the RECEIVED side-effect to mirror per line item: `product.update({ currentStock: { increment: receivedQuantity } })` (in the seed script, fold this into the final computed `currentStock` instead of a live increment) plus `stockTransaction.create({ type: "STOCK_IN", productId, quantity: receivedQuantity, reason: "Purchase Received", purchaseOrderId: id, createdById })`, then `purchaseOrderLineItem.update({ receivedQuantity })`.

`lib/utils/severity.ts`'s `getSeverityBadge(currentStock, reorderThreshold)` fixes the tier boundaries: `currentStock === 0` -> Critical; `currentStock <= reorderThreshold` (implicitly > 0) -> Warning; else OK.

`app/(protected)/dashboard/page.tsx` currently runs 5 parallel queries via `Promise.all`, then maps `poStatusGroups` through `fillPoStatusCounts` (`lib/utils/dashboard.ts`) before passing plain-object props to `DashboardClient`. The new `topSellingProducts` query follows the same two-step aggregate-then-lookup shape.

`tests/dashboard.test.ts` mocks `@/lib/prisma`'s `prisma` object with only the methods the current page uses (`product.count`, `product.fields.reorderThreshold`, `supplier.count`, `stockTransaction.count`, `purchaseOrder.groupBy`). Any new Prisma call added to `page.tsx` that isn't present on this mock object will resolve to `undefined` when awaited inside `Promise.all`, and calling `.map()`/array methods on that `undefined` will throw — the mock must gain the new methods before the 3 existing `DashboardPage` tests will still pass.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-wipe cleanup + idempotent seed script (prisma/dummy-data.ts)</name>
  <files>prisma/dummy-data.ts, package.json</files>
  <action>
Create `prisma/dummy-data.ts` as a new standalone script — its own `new PrismaClient()` instance, a `main()` function, `.catch((e) => { console.error(e); process.exit(1) })`, `.finally(() => prisma.$disconnect())`, mirroring `prisma/seed.ts`'s structure. Do NOT modify or import from `seed.ts`. Do NOT call anything on `prisma.user.deleteMany`/`updateMany` — the 2 existing users (admin@logistics.com MANAGER, karin@logis.com STAFF) must survive every run untouched.

Step 0 — full wipe (per locked decision 1), in this exact FK-safe order: `prisma.stockTransaction.deleteMany({})`, then `prisma.purchaseOrder.deleteMany({})` (its line items cascade automatically via the schema's `onDelete: Cascade` on `PurchaseOrderLineItem.purchaseOrder`), then `prisma.product.deleteMany({})`, then `prisma.category.deleteMany({})`, then `prisma.supplier.deleteMany({})`.

Step 1 — seed 4-5 Suppliers with realistic Indonesian company names (e.g. "PT Sumber Elektronik Jaya", "CV Mitra Distribusi Nusantara", "PT Cahaya Komponen Indonesia", "UD Berkah Perkakas", "PT Kantor Sejahtera Abadi"), each with a non-null `contactPerson` (Indonesian name), `phone` (+62 format), `email`, and `address` (Indonesian city). Capture the created rows' ids.

Step 2 — seed exactly these 5 Categories: Elektronik, Aksesoris, Peralatan Rumah Tangga, Komponen, Perlengkapan Kantor. Capture their ids.

Step 3 — seed 15-20 Products (aim for ~16-18) spread across the 5 categories, realistic Indonesian-distributor names, SKU convention `<CATEGORY-PREFIX>-NNN` (ELK/AKS/RT/KOMP/KAN), each with a `reorderThreshold` and an initial placeholder `currentStock: 0` (the real final value is written in Step 6, after all movements are known — Product.categoryId is a required FK so products must exist before any movement/PO generation that references them). Choose `reorderThreshold` values so the eventual severity mix (Step 5/6) lands at least 2 products at Critical (final currentStock === 0), at least 4 at Warning (0 < currentStock <= reorderThreshold), and the remainder at OK, per `lib/utils/severity.ts`'s tiers.

Step 4 — upsert 2 additional Staff users keyed on email so re-runs stay idempotent: e.g. `budi.santoso@logistics.com` / "Budi Santoso" and `siti.rahayu@logistics.com` / "Siti Rahayu", both `role: "STAFF"`, `passwordHash` via `hash("Staff@123", 12)` (bcryptjs, same cost-12 convention as seed.ts). After the upserts, `prisma.user.findMany()` to get all 4 users' live ids (2 existing + 2 new) for use as `createdById` throughout Steps 5-6.

Step 5 — design a per-product natural-movement plan (not yet written to the DB): for each product, an opening stock number and an array of `{ type: "STOCK_IN" | "STOCK_OUT", quantity, reason, daysAgo, notes? }` entries (reasons drawn from the exact enum literals above, `daysAgo` spread across 0-30). Designate 5-6 products as "best sellers" with meaningfully larger cumulative STOCK_OUT quantities (roughly 60-150 units total each across 2-3 STOCK_OUT entries) than the rest, so the Part 2 ranking is visibly non-flat; the remaining products should have little-to-no STOCK_OUT activity. Write a helper `applyMovements(openingStock, movements)` that sorts a product's movements by `daysAgo` descending (oldest first) and walks them maintaining a `running` balance starting at `openingStock`: for STOCK_IN, add the quantity unconditionally; for STOCK_OUT, clamp the applied quantity to `Math.min(quantity, running)` (skip emitting the transaction entirely if the clamped amount is 0) and subtract it from `running`. Return the final `running` balance plus the list of movements actually applied, each carrying its (possibly clamped) quantity and a `createdAt` of `new Date(Date.now() - daysAgo * 86_400_000)`. This clamp is what guarantees the DB's `currentStock >= 0` CHECK constraint can never be violated by a naive random walk.

Step 6 — seed exactly 8 PurchaseOrders: 2 `status: "DRAFT"`, 2 `status: "ORDERED"`, 4 `status: "RECEIVED"` (per locked decision requirements). Each PO gets 1-4 line items referencing distinct seeded products, realistic IDR `unitPrice` values, and a `totalAmount` computed exactly like `computeTotalAmount` in `actions/purchase-orders.ts` (`new Prisma.Decimal` sum of `quantity * unitPrice`, `import { Prisma } from "@prisma/client"`). Spread `supplierId`/`createdById` across the seeded suppliers and the 4 users from Step 4. For the 4 RECEIVED POs only: set each line item's `receivedQuantity` to its ordered `quantity` or slightly less (subtract 0-3), and for each such line create a matching StockTransaction (`type: "STOCK_IN"`, `reason: "Purchase Received"`, `purchaseOrderId` set to the PO's id, `productId`/`quantity` matching `receivedQuantity`, `createdAt` within the last 30 days), mirroring `receivePurchaseOrder`'s production side-effect. Fold each product's PO-received STOCK_IN quantities on top of its Step 5 `applyMovements` base as pure unclamped increments (STOCK_IN can never make stock negative, so no reclamping needed) — this sum becomes that product's true final `currentStock`.

Step 7 — write all Step 5 (clamped, applied) StockTransaction rows and all Step 6 PO-linked StockTransaction rows to the DB (`createdAt` set explicitly on `create` — Prisma allows overriding this normal field even though it has `@default(now())`), then `prisma.product.update` each product's `currentStock` to its Step 5+6 combined final value.

Step 8 — end the script with a summary that queries `prisma.category.count()`, `prisma.product.count()`, `prisma.supplier.count()`, `prisma.user.count()`, `prisma.stockTransaction.count()`, `prisma.purchaseOrder.count()` and `console.log`s them labeled, so every run (including re-runs) prints the current authoritative counts.

Add `"db:dummy": "tsx prisma/dummy-data.ts"` to `package.json`'s `scripts` block, directly after the existing `"db:seed"` line. Then run `npm run db:dummy` twice in a row and record the exact printed counts from the second run.
  </action>
  <verify>
    <automated>npm run db:dummy &amp;&amp; npm run db:dummy</automated>
  </verify>
  <done>Both runs exit 0 with no unique-constraint or CHECK-constraint errors; the 2 pre-existing users are unaffected; the second run's printed summary shows exactly 5 categories, 15-20 products, 4-5 suppliers, 4 users, 20-30 stock transactions, and exactly 8 purchase orders (2 Draft/2 Ordered/4 Received) — record these exact numbers in this plan's SUMMARY.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dashboard "Top Selling Products" chart (additive only)</name>
  <files>app/(protected)/dashboard/page.tsx, app/(protected)/dashboard/dashboard-client.tsx, tests/dashboard.test.ts</files>
  <behavior>
    - `page.tsx`'s new query: `prisma.stockTransaction.groupBy({ by: ["productId"], where: { type: "STOCK_OUT" }, _sum: { quantity: true }, orderBy: { _sum: { quantity: "desc" } }, take: 6 })`, followed by `prisma.product.findMany({ where: { id: { in: productIds } }, select: { id: true, name: true } })`, produces `topSellingProducts: { productId: string; name: string; totalSold: number }[]` in the same descending order the groupBy returned, `totalSold` defaulting to 0 if `_sum.quantity` is null.
    - When the groupBy returns `[]` (no STOCK_OUT transactions exist), `topSellingProducts` is `[]` and the `findMany` lookup is skipped or trivially returns `[]` — no error.
  </behavior>
  <action>
First update `tests/dashboard.test.ts`'s mock: add `groupBy: vi.fn()` under the mocked `stockTransaction` object and `findMany: vi.fn()` under the mocked `product` object (alongside the existing `count`/`fields`). In the `"DashboardPage — ..."` describe block's `beforeEach`, right after `vi.clearAllMocks()`, add `vi.mocked(prisma.stockTransaction.groupBy).mockResolvedValue([] as never)` and `vi.mocked(prisma.product.findMany).mockResolvedValue([] as never)` as defaults — this keeps the 3 existing tests passing unmodified (they never call `.map()` on `undefined`) without needing per-test edits. Then add one new test in that same describe block asserting the mapping behavior in the `<behavior>` block above: mock `stockTransaction.groupBy` to resolve `[{ productId: "p1", _sum: { quantity: 50 } }, { productId: "p2", _sum: { quantity: 30 } }]` and `product.findMany` to resolve `[{ id: "p1", name: "Product One" }, { id: "p2", name: "Product Two" }]`, call `DashboardPage()`, and assert `element.props.topSellingProducts` equals `[{ productId: "p1", name: "Product One", totalSold: 50 }, { productId: "p2", name: "Product Two", totalSold: 30 }]` (order preserved, names correctly joined).

Then implement in `app/(protected)/dashboard/page.tsx`: add the `stockTransaction.groupBy` call from `<behavior>` into the existing `Promise.all` array (6th parallel query, alongside `totalProducts`/`totalSuppliers`/`movementsToday`/`lowStockCount`/`poStatusGroups`). After the `Promise.all` resolves, derive `productIds` from the groupBy result, call `prisma.product.findMany` (skip the call or pass an empty `in` filter cleanly when `productIds` is empty — either is fine as long as the final `topSellingProducts` is `[]`), build a `Map` of id -> name, and map the groupBy result into `topSellingProducts: { productId, name, totalSold }[]` (plain objects only — no Decimal/BigInt cross the server->client boundary; `StockTransaction.quantity` is `Int` so `_sum.quantity` is already `number | null`, default null to 0). Pass `topSellingProducts` as a new prop to `<DashboardClient>`.

Then implement in `app/(protected)/dashboard/dashboard-client.tsx`: add `TrendingUp` to the existing `lucide-react` import line, and add `BarChart, Bar, XAxis, YAxis, CartesianGrid` to the existing `recharts` import line (which already has `Tooltip`/`Legend`/`ResponsiveContainer`). Add `topSellingProducts: { productId: string; name: string; totalSold: number }[]` to `DashboardClientProps` and destructure it in the component signature. Directly below the existing "Purchase Order Status" `Card` (after its closing tag, before the outer `</div>`), add a new `Card` with `<h2 className="text-xl font-semibold mb-4">Produk Paling Sering Keluar</h2>`: when `topSellingProducts.length > 0`, render a `ResponsiveContainer width="100%" height={300}` wrapping a `BarChart` (`data={topSellingProducts}`) with `CartesianGrid`, `XAxis dataKey="name"`, `YAxis`, `Tooltip`, and a `Bar dataKey="totalSold" fill="#3b82f6"`; when empty, render the same empty-state pattern as "No purchase orders yet" (a muted icon — `TrendingUp` — plus a bold message line and a muted-foreground helper line, e.g. "No product movements yet"). Do NOT modify the existing KPI tiles grid or the existing PieChart's markup, props, or click handler in any way.
  </action>
  <verify>
    <automated>npx vitest run tests/dashboard.test.ts</automated>
  </verify>
  <done>`tests/dashboard.test.ts` has 4 passing tests in the "DashboardPage" describe block (the 3 pre-existing plus 1 new topSellingProducts mapping test); `page.tsx` passes `topSellingProducts` to `DashboardClient`; `dashboard-client.tsx` renders the new Card with BarChart/empty-state directly below the PO status Card; the existing KPI grid and PieChart are byte-for-byte unchanged; `npx vitest run tests/dashboard.test.ts` exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Verification — typecheck + full test suite, zero regressions</name>
  <files>None (verification only)</files>
  <action>
Run `npx tsc --noEmit` and confirm it exits 0 with no new type errors introduced by Task 1 or Task 2 (the repo's pre-existing, separately-tracked ESLint-only `@typescript-eslint/no-explicit-any` warnings noted in STATE.md's Blockers/Concerns are out of scope here — this is a `tsc` check, not `eslint`). Run `npm test` (`vitest run`) and confirm the entire suite — all 14 files under `tests/`, not just `dashboard.test.ts` — exits 0 with zero failures. If either command surfaces a failure caused by this plan's changes, fix the root cause before considering the plan done; do not add an `any` cast or `@ts-ignore`/`@ts-expect-error`/`.skip` to force a pass.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm test</automated>
  </verify>
  <done>`npx tsc --noEmit` exits 0 with no new errors; `npm test` (vitest run, full suite) exits 0 with 0 failing tests across all test files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev machine -> Postgres dev DB | `prisma/dummy-data.ts` is a manually-invoked script (`npm run db:dummy`) that performs a full-table wipe of Category/Product/Supplier/StockTransaction/PurchaseOrder — a destructive operation if ever pointed at a non-disposable database. |
| Authenticated user -> /dashboard | The new `topSellingProducts` query runs inside the existing auth-gated dashboard route; no new endpoint, form input, or unauthenticated path is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q260709-01 | Tampering | prisma/dummy-data.ts (full-table wipe) | high | mitigate | Kept as a separate, manually-invoked `db:dummy` npm script — never added to `package.json`'s `prisma.seed` config, `postinstall`, `build`, or any CI/deploy hook; `prisma/seed.ts` (the script actually wired into `prisma db seed`) is left completely untouched, so no deploy pipeline can trigger the wipe unintentionally. |
| T-Q260709-02 | Information Disclosure | page.tsx `topSellingProducts` query | low | accept | Surfaces only product name + aggregate STOCK_OUT quantity, both already visible elsewhere in the authenticated app (Products/Stock pages); no new PII or cross-tenant data exposure. |
| T-Q260709-03 | Repudiation | prisma/dummy-data.ts backdated `createdAt` on seeded StockTransactions | medium | accept | Backdated timestamps only exist in demo/dev data produced by an explicitly-named, manually-run script that also performs a destructive full wipe — it is definitionally never run against a database whose audit trail matters, so no real audit record can be confused with seeded data. |

</threat_model>

<verification>
- `npm run db:dummy` run twice in a row both exit 0 (idempotency), leaving the 2 pre-existing users untouched, and print a final row-count summary matching the required ranges/exact split.
- `npx vitest run tests/dashboard.test.ts` exits 0 with 4 passing tests in the DashboardPage suite.
- `npx tsc --noEmit` exits 0.
- `npm test` (full suite, all files under `tests/`) exits 0.
- Visiting `/dashboard` shows the new "Produk Paling Sering Keluar" BarChart Card below the existing PO Status Card, with the KPI grid and PieChart unchanged.
</verification>

<success_criteria>
`prisma/dummy-data.ts` exists, is idempotent, and replaces all junk Category/Product/Supplier/StockTransaction/PurchaseOrder rows with internally-consistent (never-negative-stock, PO-linked-where-applicable) Indonesian-market demo data without touching the `users` table beyond adding 2 new Staff via upsert. `/dashboard` additively gains a Top Selling Products BarChart sourced from that data. `npx tsc --noEmit` and `npm test` both pass with zero regressions.
</success_criteria>

<output>
Create `.planning/quick/260709-ti6-seed-demo-data-categories-products-suppl/260709-ti6-SUMMARY.md` when done
</output>
