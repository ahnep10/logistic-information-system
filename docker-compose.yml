services:
  db:
    image: postgres:16
    container_name: logistic-postgres
    environment:
      POSTGRES_DB: logistic_mis
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
